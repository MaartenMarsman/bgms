# Master findings list — 0.2.0.0 pre-CRAN review

Maintained by the review lead. One entry per deduplicated finding.
Severity: blocker / major / minor / note. Status: open / fixed-claimed /
fixed-verified / decided / wontfix. Every fixed finding needs a verification
(commit, test, or report reference) before it becomes fixed-verified.

Seeded 2026-08-01 from the cross-repo tracker (`plans/flagged-issues.md`),
MM's A2 review log (`plans/a2-review-findings.md`), and repo state checks.
Findings from the `dev/audit/` inventory and the mapping sweeps are folded in
as reports 00a–00c and later land.

| ID | Severity | Status | Finding | Source |
|---|---|---|---|---|
| F-001 | blocker | **CLOSED** (report 14: fixed pre-review; sharpened at merge `80a28dfb`) | WAS: NEWS.md misdescribes the sensitivity check ("read from the anchor with the highest importance ESS"). RESOLUTION: already swapped for the pooling description by `45a4fae7` (#187, 2026-07-30) — the quoted text is not at HEAD; the review's inventory row was stale. Report 14 re-verified the surviving sentence line-by-line against `R/anchor_curve.R:233-251` + the man page (all accurate) and added the one omission: the pooled curve is pooled EVERYWHERE, including at anchor grid points — exactness lives on the anchor fits' own RB statistics, not on a pooled row (`R/anchor_curve.R:207-211` says so verbatim; lead-verified). Original text: NEWS ships with the release. | flagged-issues #1 |
| F-002 | major | open | Defaults-freeze memo to downstream (easybgm, JASP, docs site, tutorial) not yet sent: bgm() interaction prior CauchyâNormal(1); precision_scale_prior raw-rateâexponential-eta; new precision_graph_prior="joint"; update_method gains "gibbs"; standardize= lifecycle-deprecated; extract_ess() indicators now RB n_eff (transition ESS via estimator="mixt"); extract_inclusion_bf() gained log=. First two change numbers downstream callers quote. Report 09 (open Q5) adds the compare-side asymmetry: `bgm()` ships `interaction_prior = normal_prior(1)` while `bgmCompare()` ships `cauchy_prior(1)` â a user comparing a compare fit against separate `bgm()` fits at stated defaults is comparing different models; call it out in the memo AND one sentence in the comparison vignette. Report 14 (Q2, lead-decided): the memo ALSO carries the development-build script warning (`extract_inclusion_bf()` now returns the BF, not its log, by default) â dropped from the released NEWS as irrelevant to 0.1.6.3 users, but JASP/easybgm track develop and must hear it through this memo. Report 15 (D-11) confirms the comparison-vignette sentence STILL absent â it rides brief 17; the memo itself remains MM’s Phase-2 send. VIGNETTE SENTENCE LANDED (brief 17, merged ec1c91a4): comparison.Rmd now states the bgm-normal vs bgmCompare-Cauchy default asymmetry and the match-priors remedy; the memo remains the open remainder. | flagged-issues #2; report 09 Q5; report 14 Q2; report 15 D-11; report 17 |
| F-003 | blocker | open | DESCRIPTION `Date: 2026-03-26` is stale (guaranteed CRAN NOTE). Bump in the submission commit, not before. | flagged-issues #5 |
| F-004 | minor | fixed-verified (merged, brief 12) | README development-install line lacks `@develop` ref — plain `install_github` call installs main. FIXED (`b6f237b5`): install line now targets `@develop`. Merged `f24ad3c8`. | flagged-issues #5 (A2 pass); report 12 |
| F-005 | minor | open | `prior_sensitivity_check()` roxygen Details needs one framing sentence before the bare `\insertRef{bartos2026}`: Bartos et al.'s reweighting identity is used locally around refit anchors; the anchored construction is the package's own. MM approves wording; regenerate Rd. | flagged-issues #5 |
| F-006 | minor | open (downgraded; residue (a) → brief 17) | Report 15 §5: an INDEPENDENT read of `src/mcmc_diagnostics.cpp:375-387` agrees with the lead's corrected reading, and the rewritten vignette matches the code — substance confirmed fixed twice over. New code-side sibling found in the same read: D-10 → F-095. The wrong mixture-ESS gloss is ALREADY FIXED in `vignettes/diagnostics.Rmd` source (rewritten by `b42df13f`, #190; verified in report 00c). Residue: (a) soft gloss at `vignettes/diagnostics.Rmd:88` — flip counts framed as "how much the chain explored the two states", the weak survivor of the rejected reading; (b) stale pre-build artifact `doc/` dir still carries the old text (`doc/diagnostics.Rmd:68-72`; `.Rbuildignore`d, but delete to prevent confusion). | a2 #1; report 00c §6 |
| F-007 | minor | open | `Readme.Rmd` stale vs hand-edited `README.md`; knitting would regress the README. Decide: drop the Rmd (recommended) or re-sync. | flagged-issues #12 |
| F-008 | note | open | `.github/copilot-instructions.md` gap after pkgdown removal: "register a new exported function" procedure should point at the bgms-docs reference workflow. | flagged-issues #13 |
| F-009 | note | decided | Build hygiene: never build/check in the Dropbox tree (stale `src/*.o` against old RcppParallel + generated `src/Makevars` → unloadable `bgms.so`; Dropbox sync resurrects files mid-render). Standing practice: `git archive` to a clean dir outside Dropbox for every build. | flagged-issues #16, #14; A5 §2 |
| F-010 | decision | SUPERSEDED (MM, 2026-08-03) — `precision_graph_prior` DEFAULTS TO "hierarchical" for 0.2.0.0; brief 24 (after 13) | The 2026-07-30 stay-joint resolution (flagged-issues #2b) is superseded: ALL FOUR grounds dissolved on re-examination — (1) the gauge is ON by default on current develop (roxygen `R/bgm.R:237` "The gauge runs by default"; post-sampling auditing is its design, not a defect); (2) the silent clamp was LIFTED in #193, review-verified (00a row PR172-B1 "fixed (superseded) — cap 80, boundary-slope extension, counters"): beyond the trained hull, predictions follow the boundary slope instead of freezing at the edge, every extrapolated evaluation is counted (n_extrap, max size, retained-vs-warmup), and it is no longer silent — `zratio_extrapolation_notice()` reports the share with honest semantics (warmup-only transients named as such, "stored draws are unaffected"; the complete-graph initialization makes early mediating blocks ~q by construction), tested in `test-zratio-extrapolation-notice.R` incl. the T2 size-cap fit that proves a past-hull fit end to end (448 s, report 12); the cap-tier top-up guarantees hull coverage to cap−2 and "measured hull accuracy at the top tiers is unaffected"; the only clamps remaining are input sanitation (log-moment ranges ±0.1) and a measured ~2e-6 table-edge clamp; (3) the "minutes-order surface-build pause" is stale — certification Task D measured a full hierarchical Wenchuan fit at 6.16 s, first `verdicts()` 0.26 s; (4) slab conditionality is moot — the default slab (normal, scale 1) is supported, and the beta-prime corner is unused (MM product judgment). The "flip would be a breaking change" phrase was WRONG under the never-shipped rule: no `precision_graph_prior` ever shipped (the GGM class is new in 0.2.0.0), so NEWS describes hierarchical-as-default as a FEATURE, no change entry. Affirmative ground (MM): hierarchical delivers the prior inclusion probability AS SPECIFIED — joint requires the effective-prior extractors to learn the prior actually applied. The path carries the review's deepest certification (gold bank, route certificates, gauge detector tests) plus MM's own two-day review — the flip ships that work as the default rather than shelving it. BRIEF-24 SCOPE: flip the default wherever the argument exists (continuous + mixed); explicit, message-carrying behaviour for unsupported slabs; F-022 wiring REQUIRED (NA harm on mixed would otherwise become the default experience); roxygen/docs sweep; full suite under the new default with suite-time impact measured; NEWS feature wording; F-002 memo line updated ('precision_graph_prior default "hierarchical"'); docs-site fixtures need a re-bake post-flip (site launch chain flag). | MM 2026-08-03; lead verification of grounds (code + Task D) |
| F-011 | note | fixed-verified | pkgdown decommissioned before any release tag: `.github/workflows/pkgdown.yaml` and `_pkgdown.yml` absent from rc1 (verified in merge diff, 2026-08-01). Required because the workflow also fired on published releases. | flagged-issues #4/2; verified in rc1 |
| F-012 | major | fixed-verified | CLOSED by report 01 §f1: the in-repo anchor is byte-identical to the published CRAN 0.1.6.3 tarball modulo build-time artifacts (32 trailing-newline-only diffs + DESCRIPTION fields added at build). Every review diff stands on a sound baseline. | report 01 f1 |
| F-013 | note | fixed-verified | CLOSED: main + both release tags pushed 2026-08-01 at MM's direction; the two stale pre-review tags deleted (recovery SHAs in the details section). Remote state verified. | review lead, 2026-08-01 |
| F-014 | note | open | Branch hygiene: six unmerged local branches. Verified 2026-08-01 that all release-relevant content is in rc1: `fix/bgmcompare-association-scale` and `feature/indicator-diagnostics-cleanup` are patch-equivalent in develop (`git cherry` = 0); `feature/hier-followup`, `feature/hier-gating`, `feature/user-facing-checks` residual diffs are pre-squash/pre-refactor states (develop strictly newer — checked hunks). `feat/graphical-g-prior` (May 2026, +14k/−37k vs develop) is the deliberate post-release GGM line — keep. Delete the five absorbed branches after MM confirms; retire the stale `wt-ufc` worktree on user-facing-checks. | review lead, 2026-08-01 |
| F-015 | note | fixed-verified | The 2026-07-31 scale-fix landing decisions (`dev/audit/2026-07-31-scale-fix-landing-decisions.md`) were executed: (1) fix landed as its own commit, patch-equivalent in develop; (3) the scale-contingency sentence is in `vignettes/comparison.Rmd:70-75`; NEWS.md "Breaking changes" documents the omega*x vs 2*omega*x divergence and the ~½ rescaling of reported pairwise effects. Item (2) verified by report 01 f12: the guard (`test-bgmCompare.R:337`) runs in BOTH tiers at 2.2 s. Fully closed. | review lead; report 01 f12 |
| F-017 | major (regraded from blocker) | fixed-verified | `.Rbuildignore` gaps, measured by report 01 f3: `tests/compliance/` = 2.85 MB = 66% of the shipped tests/ payload, never executes under R CMD check (check runs only top-level tests/*.R); `tests/fixtures/` ships an orphan generator. No CRAN check trips (installed 4.6 MB, tarball 4.31 MB, size checks OK) → hygiene/honesty issue, not a submission trip; regraded to major, fix stays in the submission batch: add `^tests/compliance$`, `^tests/fixtures$`. Correction: `tests/testthat/_problems/` does NOT exist in rc1 — that clause is already satisfied. FIXED in brief 05 (`50281f7a`): `^tests/compliance$`, `^tests/fixtures$`, plus `^\.git$` hardening; tarball delta exactly the 39 expected entries, nothing else in either direction, 4.52 → 1.58 MB; as-cran unchanged (2 baseline NOTEs); the `.git`-file case verified empirically on a planted worktree-style `.git`. Merged `b04dbd06`. | 00a AUD-R1; report 01 f3; report 05 |
| F-018 | blocker (upgraded) | fixed-verified | CRAN 2-core policy violation, measured by report 01 f4 — the earlier "examples capped" claim was WRONG: 76 uncapped fit calls across 26 Rd files run 4 chains; a default example call sustains 3.9 concurrent cores and `_R_CHECK_LIMIT_CORES_` changes nothing on the sampler path (`R/run_sampler.R:365` passes `s$cores` straight through), while `R/correction_tables.R:151` already implements the correct guard (`normalize_builder_cores`). Vignettes (show-uncapped/run-capped pattern) and tests (≤1.3× ratio) are clean — examples-only. Fix shapes: (a) teach `run_sampler` to honour `_R_CHECK_LIMIT_CORES_` like `normalize_builder_cores` (one code change, fixes all 76 examples, recommended), (b) cap chains/cores per example, or both. MM confirmed the blocker and shape (a); FIXED on `fix/checking-layer-batch` (`a6c0725d`): shared `normalize_parallel_cores()` applied at `validate_sampler()`, simulate/predict, `refit_engine`, and the surface build; measured 3.93 → 1.98 under the env var, 3.93 unchanged without. Deliberate deviation, flagged: the Windows `return(1L)` fork rule stays builder-only (sampler workers are threads). Merged to develop `adf87013`. SCOPE NOTE (report 17 finding 3): all 23 extractor examples (incl. F-031's two new ones, deliberately matched to their siblings) inherit uncapped `cores = parallel::detectCores()` — if F-018's guarantee is meant to reach EXAMPLES and not only the sampler env-var path, the 23-family needs one sweep (backlog/13 candidate). | report 03 §3.5; report 17 finding 3 |
| F-019 | major | fixed-verified (merged `8bfa6148`, brief 13 — REAL DEFECT: untilted `sample_ggm_prior(update_method="gibbs")` threw chol() failures on 2/60 seeds at p=15, 5/60 at p=25, guarded Metropolis 0/60 same cells, 300 with-data runs 0; instrumented build attributed 6/7 throws to the conjugate edge move — negative smallest eigenvalue PROPOSED AND ACCEPTED; guard added mirroring the existing two, regression test proven to bite; `c6a9c40c`. LEAD RULING: NO NEWS clause — the defect never shipped, no src/models/ggm exists at the tag; a 0.2.0.0-reader draft sits in the report if MM overrides) | AUD-H6 (July audit) is the ONLY Phase-1 correctness item with no visible landing: no n_==0 positive-definiteness guard inside `update_edge_indicator_conjugate` (`src/models/ggm/ggm_model.cpp:995`), though `ggm_edge_move`/`ggm_diag_move` have guards (`:288`, `:682`). Settle by targeted read + a long `sample_ggm_prior(update_method="gibbs")` run. | 00a Top-10 #4 |
| F-020 | major | open | SLAB-2: no cross-chain conditional-means check on slab weights in `R/mcmc_summary.R` — the one detector with measured full power for weight multimodality where indicator diagnostics are structurally blind (slab-diagnostics brief). Decide: implement for 0.2.0.0, or ship-later with a NEWS/docs caveat. MM decision. | 00a SLAB-2 |
| F-021 | minor | fixed-verified | `calibration_check.bgmCompare` exists as a 19 KB patch (`dev/audit/2026-07-30-b4-calibration-bgmcompare.patch`), never applied; NAMESPACE has only the `bgms` method. MM CORRECTED the lead's stub reading (2026-08-01): "I would actually ship a calibration check for bgmCompare" — 0.2.0.0 ships the METHOD. Complication the lead verified: the patch predates the review and collides with the F-035 rewrite (`0131bcdc`) — it carries parallel `category_levels` decode helpers and edits the old silent-NA site, so it must be ADAPTED onto the shipped `recode_simulated_to_original()`/`discrete_category_index()` helpers, not applied. Brief 06 item 5, with F-035-standard verification (round-trip incl. non-contiguous scores, per-group curves, layout regression test); graceful stub was the pre-authorized fallback. SHIPPED IN FULL (`82edaa4c`, brief 06): reconciled onto the shipped decode — a compare branch in `fitted_observed_data()` restores the BC baseline, `discrete_category_index()` learns the compare many-to-one lookup, panel builders shared with the bgms method, F-040 pagination kept; F-035-standard round-trips (non-contiguous scores, BC baselines, layout-divergent lookup); per-group Boredom margins match observed to 0.0070/0.0098. The work surfaced F-068. Merged `4969f843`. | 00a PAR-B4; report 07; MM 2026-08-01 |
| F-022 | minor (REQUIRED in brief 24) | open — rides in the F-010 flip batch: with the gauge on by default and hierarchical as the default prior, an unwired `harm_inputs` would make NA harm the default mixed experience | Mixed hierarchical fits: `harm_pred` is permanently NA — `R/build_output_mixed_mrf.R:305` calls `summarize_zratio_gauge()` without `harm_inputs`. Wire the inputs or state the per-model limitation in the Rd/vignette. | 00a PR172-open |
| F-023 | minor | fixed-verified (merged, brief 14) | NEWS lacked a `simulate_mrf()`-specific sentence for the factor-2 input-scale change. CLOSED with the mechanism verified in source both sides (`src/mrf_simulation.cpp:97` `2.0 *` vs `cran-0.1.6.3:...:98` no factor): Breaking-changes bullet states the change, the user action (halve a hand-specified matrix to reproduce 0.1.6.3 output), and the exemption (matrices from 0.2.0.0 fits need no adjustment; the continuous branch takes a precision matrix, unaffected). | 00a AUD-R3 |
| F-024 | note | fixed-verified | The uncommitted 10-line edit to `tests/testthat/test-zratio-surface-build.R` was committed on develop as "test(zratio): stop paying for constants and anchors the surface-build claims do not use" (2026-08-01, now `dd43696a` after the history repair). Note: this test change postdates rc1 — the rc1 tag intentionally does not contain it. | 00a; resolved 2026-08-01 |
| F-025 | minor | open | Verify shipped wording on the two decided-open hierarchical limitations: (a) sub-shape-0.5 additive zero-collapse — NEWS/`?bgm` text vs the measured 12–32-variable boundary (`dev/plans/active/2026-08-01_hier-followup_NOTE.md` Disposition); (b) the eta<2 gate deviation record lives only in ggm_paper's `deployment-approach.md` — confirm it was updated or copy the decision in-repo. | 00a HF-1/PR172-B2 |
| F-026 | minor | partial-fixed (minimum merged, brief 17; full rewrite = MM/Phase-2 decision) | `vignettes/intro.Rmd` untouched since 2025-07-01: predates the 0.2 API (no prior constructors, no verdicts, no `precision_graph_prior`, no mixed-MRF mention), only vignette without a bibliography header. The front-door vignette misrepresents the package. Report 15 confirms (renders clean; "not wrong, incomplete") and adds two concrete defects 17 fixes: the variable-type sentence omits the mixed case (a fourth, unlisted class), and the only GGM demo chunk is unrunnable as written (undefined `continuous_data`, `eval = FALSE`). 17 also adds a short what's-new pointer paragraph; the full intro rewrite stays open — MM's voice. | 00c §6; report 15 §3.1 |
| F-027 | minor (regraded) | open | `SystemRequirements: GNU make` absent while `src/Makevars.in` uses `include sources.mk`. Report 01 f9: `checking for GNU extensions in Makefiles ... OK` on macOS — `include` is portable across make implementations, so the expected trigger did not fire. Settle on win-builder/r-hub (Phase 3); add the field only if flagged there, else drop. | 00c §7; report 01 f9 + OQ4 |
| F-028 | minor | open | `RoxygenNote:` removed from DESCRIPTION, replaced by `Config/roxygen2/version: 8.0.0` (ahead of CRAN's roxygen2 line). Unconventional; confirm the Rd regenerate cleanly with a released roxygen2 and restore the conventional field. | 00c §7 |
| F-029 | major | fixed-verified (merged, brief 14) | Silent behavior changes for existing users whose NEWS coverage must be verified line-by-line during NEWS reconstruction — DONE: report 14's checklist shows SEVEN of the eleven had NO entry at all (`iter`/`warmup`, `target_accept`, `means_prior`, `threshold_prior`, `difference_family`, `progress_callback`, compare RB PIPs returning NA); all eleven now carry a cited entry. Original list: `iter`/`warmup` defaults 1e3→2e3; NUTS `target_accept` 0.60/0.65→0.80; `hamiltonian-mc` + `hmc_num_leapfrogs` removed; new formals `means_prior`, `threshold_prior`, `delta`, `progress_callback`, `difference_family`; `extract_ess` default now RB with no warning on new fits; prediction/simulation on the ×2 association-scale convention; bgmCompare RB PIPs return NA for unselected indicators (breaks all-numeric assumption downstream — add to the F-002 memo). | 00c §3; 00b |
| F-030 | major | fixed-verified (closed, no action) | CLOSED by report 01 f8: CRAN-mode suite is 67 s standalone / 65 s in-check (CPU/elapsed ratio 1.21, so ~80–90 s on a 2-core CRAN machine). Order of magnitude inside budget; no CRAN-tier gate needed. Determinism verified: two NOT_CRAN runs matched pass-for-pass across all 77 files. | report 01 f8 |
| F-031 | minor | partial-fixed (two examples merged, brief 17; wider 25-file sweep = backlog) | Rd polish before submission: `mrfSampler` is a live export with `\keyword{internal}` and no `\value` (CRAN checks exported Rd for \value); 25 Rd files lack `\examples{}` incl. 12 exported extractors; `withr` sits in Suggests — confirm used or drop. Report 15: of the 23 NEW exports, exactly 2 lack examples (`extract_inclusion_bf`, `extract_prior_inclusion_probabilities`; all 23 have `\value`) — 17 adds those two following the sibling-extractor pattern (small fit, `\donttest` if siblings use it — mind F-018); the wider 25-file sweep remains a submission-batch decision. | 00c §2,7; report 15 §6 |
| F-032 | note | open | C++ debris (backlog, not release): dead `// [[Rcpp::export]]` at `src/math/cholupdate.cpp:123` (never scanned; function dead weight); 45-line commented-out harness at `src/utils/progress_manager.cpp:406-450`; `src/models/ggm/zratio_law.h` (501 LOC) dormant-by-design but compiled into the shipped binary (documented insurance — keep, but note). | 00b §17 |
| F-033 | note | open | Engineering-risk flags for Phase-1 expert reads (no confirmed bug): adaptation controllers hold reference members (`src/mcmc/samplers/metropolis_adaptation.h:19` `arma::mat&`; `NUTSAdaptationController` holds `&schedule`) — lifetime discipline to verify; `base_model.h` has three parameter vectorizations with defaulted fallbacks (silent mis-storage risk); `thread_local` scratch inside the bgmCompare TBB worker (`bgmCompare_logp_and_grad.cpp:604-605`). | 00b §5,6,11 |
| F-034 | note | open | bgmCompare is the only sampler path NOT on `mcmc/execution/chain_runner` and the only one with NO test interface — its new `CompareSweepState` caching is reachable only end-to-end. Elevates Phase-1 row 2 (cross-path consistency validation is the primary defense); the BaseModel port is backlog AUD-D2. | 00b §1,15; 00a AUD-D2 |
| F-035 | major | fixed-verified | `calibration_check()` crashed on Blume-Capel and mixed fits. Hypothesis confirmed: BC columns record an additive shift, not a recode map — `category_levels[[j]]` is NULL (`reformat_ordinal_data()`, `R/validate_data.R:246-322`); the suspected mixed name-misalignment is NOT a defect. PLUS a second, SILENT site the crash masked: `match(newdata[, v], NULL)` → all-NA → silently wrong calibration curve. Both fixed (`0131bcdc`): decode via `recode_simulated_to_original()` + `discrete_category_index()`; round-trip tests incl. non-contiguous ordinal scores. Merged to develop `adf87013`. | report 03 §2.1 |
| F-036 | major | fixed-verified | `verdicts()`/`extract_inclusion_bf()` on a hierarchical fit silently runs a 5000-iteration prior-only chain (`R/extractor_functions.R:466` → `extract_prior_inclusion_probabilities()` → `prior_only_chain_pips()`, iter=4000 + warmup=1000): minutes-long hang with no message or progress. MECHANISM CORRECTED by reports 03+04, and it is a WRONG NUMBER, not just a slow one: no prior chain runs on this config — the 2 minutes was `build_ggm_correction_table()` (the JOINT spec's tilted sweep, 68 s cold at q=17), and the joint prior PIP (0.4219) was served to a HIERARCHICAL fit whose spec prior is exactly 0.5 — inflating every hierarchical inclusion BF by +0.315 nats. Closed-form fix on `ff29d177` (isolated commit; MM confirmed the math 2026-08-01: per-graph normalization ⇒ graph marginal = nominal edge prior exactly; documented in `?bgm`, asserted by #193's test). Interrupt question closed: interrupts are swallowed, table is bit-identical to clean runs, cache cannot be poisoned, survives saveRDS. Correction from 04 Task D: the real 5000-iter prior chain is triggered by the SBM edge prior (~1.4 s at p=26), joint and hierarchical alike — not by the precision-graph prior. | report 03 §2.2-2.3; report 04 §6 |
| F-037 | **blocker** (upgraded) | fixed-verified | Mixed-fit `verdicts()` attributed EVERY number to the wrong edge: labels built in row-major variable order, numbers in the fit's block layout (dd, cc, cross) — 136/136 rows mismatched on MM's fit; intrusion–dreams (true log BF +122.5) reported as absence at −1.33. Same misalignment poisoned `plot.bgms(type="network")` edge weights and the fragility flag (mcse/draws in block order). Modelling itself is FINE: GGM vs 16-continuous mixed agree (median |Δ log BF| 0.103, cor(PIP) 0.997). Unambiguous mechanical bug → fixed (`3b42a67c`): `indicator_pair_index()` reads positions off the fit's own layout; layout-divergent regression tests. GGM/ordinal fits were unaffected (orders coincide). Merged to develop `adf87013`. | report 03 §2.4 |
| F-038 | minor | fixed-verified | `plot_edge_posterior()` title formats the raw BF with `sprintf("%.1f")` (`R/plot_bgms.R`, title block) — a decisive edge with log10 BF ≈ 130 prints a ~131-digit integer in the plot title (MM: "massive string of numbers" — the BC fit's log10_bf 130.9 printed a 131-digit integer). FIXED with F-039 (`20b56996`): titles print natural-log "log BF = 122.5", capped as "log BF > 10,000" (cap only ever fires on ±Inf in practice; snapshot-tested). Merged to develop `adf87013`. | report 03 §3.3 |
| F-039 | minor | fixed-verified | `print.bgms_verdicts` invites a base confusion: the column is `log10_bf` but the header names only "Bayes factor of 10 (and 0.1 for absence)" — MM himself read −1.474 as natural log and reported a classification bug. Verified: classification is CORRECT in log10 (`build_verdicts`: `lthr = log10(evidence_threshold)`, boundaries ±1). IMPLEMENTED (`20b56996`, merged `adf87013`): `log_bf` natural-log column package-wide (log10_bf gone except sensitivity check), boundaries ±log(threshold) printed in the header, `boundary_distance()` conversion cancels so fragility values are unchanged, calibration figure restated (0.25 log10 → 0.58 nats). Residue RESOLVED BY DECISION (MM, report 07 Q3 — the in-session mix is "jarring": "Lets make sure we have only natural log everywhere"): convert `prior_sensitivity_check()` to natural log package-wide, brief 06 item 8, per the report 03 §5 site inventory; tuned constants re-express EXACTLY (0.5 log10 → 1.1513 nats, 3 log10 → 6.9078 nats, plot cap 6 log10 → 13.8155 nats) so behavior is unchanged; tutorial figures (log10) flagged downstream via the F-002 memo. CONVERSION SHIPPED (`4cc8ce41`, brief 06): every report-03 §5 site in nats (`chosen_scale_log_bf`, `$log_bf`, `$log_bf_mcse`; 0.5→0.5·ln10, 3→3·ln10, cap ln(1e6)≈13.8); invariance EXACT (curves = old × ln 10 to 1.8e-15, all classifications identical); no deprecation duplicate (0.2.0.0 unreleased, `20b56996` precedent). Merged `4969f843`. Fully closed. | report 03 §3.4, §5; report 07 |
| F-040 | minor | fixed-verified | (a) units now stated ("proportion … 0.3 is 30%, not 0.3%"). (b) Correction: `variables =` ALREADY SHIPPED in 0.2.0.0 — MM's complaint was discoverability; added `max_panels` (default 9, worst-first) + `page` with a guidance footer (`aff555f7`). Merged to develop `adf87013`. | report 03 §3.6 |
| F-041 | note | fixed-verified | `finite_reduce()` now guards the empty/all-NA reductions in `refit_convergence_gate()` — an unassessable criterion abstains instead of leaking warnings and ±Inf pseudo-verdicts (`0972109a`). Refit-iteration floor CONFIRMED intended and documented (warm NUTS refits use the validated 500/1000 schedule regardless of source fit; `?prior_sensitivity_check`). Merged to develop `adf87013`. | report 03 §3.6 |
| F-042 | minor | closed (five dead blocks deleted `f32bdb30`, merged `8bfa6148`; live sections 7–8 untouched) | Five tests can NEVER run in any configuration: `test-simulate-predict-regression.R:225-332` resolves golden fixtures to `dev/fixtures/scaffolding/`, which was removed when `d55f3916` untracked dev/ — no generator exists in the repo. They report as skips ("deliberately deferred") but are dead. MM DECIDED (2026-08-03): delete — "If unused, we can remove it." Brief 13 task 6 executes (sections 7/8 of the file stay — live). | report 01 f5 + OQ3; MM 2026-08-03 |
| F-043 | note | open | Shipped-tarball self-test is weaker than the repo's: `tests/testthat/fixtures/` is `.Rbuildignore`d, so 3 zratio-engine tests + the legacy-fit extractor tests skip on CRAN with a misleading "not generated" message (the file was generated, then excluded). Defensible trade; reword the skip message. | report 01 f6 |
| F-044 | note | open | Test-adequacy statement: ALL SBC certification, parameter recovery, and NUTS-vs-MH cross-validation (8 files, 96 tests) sit behind `BGMS_RUN_SLOW_TESTS`, which only the twice-weekly nightly sets — never on PR, never in R CMD check, never for users. Design choice, not defect, but the release sign-off must not assume R CMD check exercised them. Brief 04 ran them (report 04): 8650 pass / 4 deterministic fails, all four test-side (F-048, F-049); SBC scoping verified CORRECT throughout the suite (04-3: sbc-ggm all-continuous; sbc-correction restricts with-data cells to exact-Gaussian models, header says why). THE RISK MATERIALIZED: three contradictory assertions merged unexecuted because nightly-validation has no push/PR trigger; first red nightly = Mon 2026-08-03 unless brief 05 lands first. Open process decision: should the slow tier gate PRs (or at least release branches)? | report 01 f7; report 04 §04-1/04-3 |
| F-045 | note | open | Build environment notes: `R CMD build` FAILS without pandoc on PATH (all five vignettes knit live; none cached) — machine property, worth a line in MAINTAINERS; vignettes run live MCMC at build with the capped show-uncapped/run-capped pattern (78 s CPU total) — preserve the pattern when editing vignettes. Compilation is pristine: zero warnings across 43 TUs at -Wall (report 01 f9). | report 01 f11/f9 |
| F-046 | note | open (MM decision) | `plot.bgms(type = "network")` returns a qgraph object; qgraph sits in Suggests and drags a heavy dependency tree. MM flags the tension. Options: keep Suggests + graceful no-qgraph message (status quo, CRAN-standard), or add a base-graphics fallback renderer. Not release-blocking. | report 02 (MM) |
| F-047 | note | fixed-verified | Same-variable error now names the resolved variable and suggests the fix (`1aa527b4`, merged `adf87013`). The `extract_centrality()` positioning note goes to the docs pass. | report 03 §3.6 |
| F-048 | major | fixed-verified | Three slow-tier assertions encode the PRE-#193 fence and are contradictory as authored: `test-zratio-surface-build.R:149-157` expects NULL surfaces at shapes 2.5/5 — inside the same PR's shipped fence [0.5, 10] — and `test-zratio-gauge.R:213`'s fixture premise ("shape 2 falls back to additive") is falsified by deployment; causally confirmed — under the shipped fence the shape-2 surface CUTS projected distortion 5-fold and the harm flag correctly stays down (fixture stale, channel healthy). Never executed pre-merge because nightly-validation has no push/PR trigger. REPAIRED in brief 05 (`d29989cd`): fence endpoints read off the shipped constants and asserted at 0.5/2.5/5/10 (build) vs 0.25/12 (NULL); gauge fixture factored by shape — firing test moved to shape 0.4 (the additive band), asserting harm_pred > threshold, flag TRUE, AND flip_rate quiet; shape 2 kept as the negative control reproducing report 04's measurement to the digit (17.19 / 0.00794 / FALSE). Harm channel verified in both directions on one fixture. Merged `b04dbd06`. Residue → F-059. | report 04 §04-1; report 05 |
| F-049 | major | fixed-verified (merged, brief 12) | `test-prior-sensitivity.R:432` fails deterministically: difference-scale reweighting vs independent refit gap = 0.0255 where the gate allows 4×(two-refit noise) = 0.0202 (5.05× vs 4×). The gate's threshold is itself a 2-sample spread estimate — fragile by construction. MM decides: substance (reweighting biased ~0.005 pip?) or tolerance; proposed cheap diagnostic: ~20 seed-triples, look at the ratio distribution. bgmCompare difference path = pseudolikelihood, so this cross-validation IS its proper gate (04-3 confirmed SBC suite correctly GGM-scoped). SEQUENCING REVISED (lead, 2026-08-01): F-061 sits in the SAME reweighting path — `refit_engine` returns `edge` (names layout) beside `pip` (RB draw layout) in one object — so the test's `pairwise` indexing may compare misaligned entries, and a 0.0255-vs-0.0202 gap is exactly the signature a few misaligned edges would leave. Decision PAUSED: brief 06 item 1.5 re-runs this test after the F-061 fix; if it passes, F-049 closes as downstream-of-F-061. MM CONFIRMED the sequencing AND PRE-APPROVED the ~20-seed-triple diagnostic if the gate still fails (2026-08-01) — wired into brief 06 item 1.6. RESULTS (report 06): the lead's layout hypothesis REFUTED (all layouts verified aligned, 36/36 and 10/10; F-061 was never on this path); gate still fails at 5.052 (test moved to `test-prior-sensitivity.R:460` by the batch's insertions). The 20-seed diagnostic finds BOTH effects: every seed's gap exceeds its noise (ratios 1.02–6.72, median 2.78 ⇒ a real ~0.01-pip reweighting bias at the 2× extrapolation, worst pairwise edge, consistent with IS error growing with extrapolation — the two lowest-ESS triples gave the two worst ratios), AND the 4×-noise gate trips on only 4/20 seeds with the shipped seed at q90 (threshold luck; its 0.0255 gap exceeds all 20 fresh-seed gaps 0.006–0.017). DISPOSITION OPEN (MM): (a) accept for 0.2.0.0 — document the ~0.01-pip 2×-extrapolation bound in `?prior_sensitivity_check` and re-found the gate (absolute-tolerance from the seed study, or pool noise over ≥4 refit pairs); (b) chase the bias (IS diagnostics) post-release. Lead recommends (a) now, (b) backlog. MM CONFIRMED (a) (2026-08-01, "I follow your advice"): gate re-founding + the Rd bound land in brief 12 — lead-chosen shape: pool the noise over 4 refit pairs, keep the 4× semantics, deterministic seeds; the test itself moves to the weekly certification tier under the F-071 respec. Bias-chase recorded as `dev/plans/backlog/future_tasks.md` item 40. RE-FOUNDING PARTIAL (brief-12 status relay, 2026-08-01): over 8 fresh seed bases the approved construction is still fragile — pooled denominator alone leaves 2/8 over the ×4 gate, pooling the reference too leaves 1/8; the residual variance sits in the SINGLE ANCHOR FIT, which neither construction pools. Agent shipped the better construction at the maintainer's ×4 with the failure mode documented in-test (no quiet gate-widening — correct conduct). LEAD AUTHORIZATION (2026-08-01): additionally pool the anchor fit (average 2–4 deterministic-seed anchor refits) — the test is T2 now and affords the runtime; target 8/8 seed bases green; if unreachable, the shipped shape + documentation stands. INTEGRATION CHECK at 12's merge (from report 14): the brief-12 Rd sentence must state exactness the way the code does — exactness lives on the anchor fits' RB statistics, NOT on the curve row at the anchor scale (the pooled curve is pooled everywhere, `R/anchor_curve.R:207-211`); harmonize with the NEWS sentence merged in brief 14. SHIPPED (`8ef96121` + `0f488b46`, merged `f24ad3c8`): 3 pooled anchors + 8 pooled refits — 8/8 seed bases under the ×4 gate (median 1.55, worst 2.83, 29% headroom); pooled gap 0.0018–0.0088 pip inside the documented ~0.01 bound; 11 fits/run, T2. INTEGRATION CHECK PASSED (lead, at merge): the Rd sentence states exactness on the anchor fits' own RB statistics with the between-anchor curve importance-reweighted (deviation up to ~0.01 pip at extrapolation ends) — matches `anchor_curve.R:207-211`; brief 17 task 13 is now a cross-file wording verify only. VERIFY DONE (17, merged): Rd and NEWS AGREE on where exactness lives; no edit needed (report 17 task 13). | report 04 §04-2, §04-3; MM + lead 2026-08-01; report 12 finding 4; lead merge check 2026-08-02 |
| F-050 | minor | fixed-verified | The entire slow tier is unmigrated to 0.2.0 argument names: 1152 deprecation warnings (`pairwise_scale`, `main_alpha`), zero substantive. MIGRATED in brief 05 (`afd68b56`), all 1152 occurrences via the same mapping `bgm()` applies internally; equivalence verified BITWISE on a GGM and a mixed cell (draws identical both ways); slow tier now 0 warnings. Merged `b04dbd06`. | report 04 §04-4; report 05 |
| F-051 | minor | fixed-verified | Sensitivity plot vs table inconsistency: the curve clamps pip at 1−1e-6 → log10 BF capped at 6.0, while `$edges` reports uncapped RB values — 93/136 edges differ (max 8.26 log10), 11 clamped edges are not marked `saturated` (that flag means zeroflip, a different concept). No verdict changes (0/136). Docs line SHIPPED in brief 05 (`8e76b87e`, four sentences in `?plot.bgms_prior_sensitivity` Details; Rd regenerated), merged `b04dbd06`. The block was rewritten in nats by the conversion (`4cc8ce41`; cap now ln(10^6) ≈ 13.8). Fully closed. | report 04 §04-5; report 05 |
| F-052 | note | decided | Standing, banked disagreement in the anchor gate: in 5/20 cells the gate re-derives a 4× anchor-budget multiplier where `zratio_anchor_shape_multiplier()` ships 2×; in one cell (eta 2, cn, shape 0.5) parity is unmet even at the 4× cap. Identical in the bank — predates rc1. MM DECIDED accept-and-record (2026-08-01); decision record written as MAINTAINERS.md law #12 (certificates are the authority; revisit triggers: a certificate fails, or the GGM paper reopens the derivation). CLOSED. | report 04 §04-7; MM 2026-08-01 |
| F-053 | minor | open | No bgms computation can be interrupted: `Rcpp::checkUserInterrupt()` exists only in `mrf_simulation.cpp`; SIGINT during any chain/sweep/surface build is swallowed until the C++ call returns (demonstrated on a 69 s sweep). Benign for correctness (completed results are bit-identical); bad UX on long fits. C++ backlog item — add interrupt checks at safe points in the chain loops. | report 03 §2.3, §4.3 |
| F-054 | minor | fixed-verified | Correction-table disk cache key omits the package version (`ggm_ctable_v1_...`) unlike the surface cache (`zratio_surf_v2_0.2.0.0_...`); observed cross-version contamination: rc1-built vs develop-built tables share a key and differ (max edens diff 0.042). FIXED in brief 05 (`7d24331e`): `ggm_correction_table_key()` mirrors the surface-cache convention, version right after the schema tag (`ggm_ctable_v1_0.2.0.0_...`); regression test plants a sentinel under a wrong-version key and asserts it is never served; old-key files orphaned, not migrated. Merged `b04dbd06`. | report 03 §4.2; report 05 |
| F-055 | blocker-candidate | fixed-verified | `bgm(variable_type = "continuous", precision_graph_prior = "hierarchical")` CRASHES at q = 2 or 3: the bipartite anchor grid's smallest size is 4, so `zratio_anchor_grids(cap ≤ 3)` returns 0 rows and `bip_jobs$fam = "bip"` (`R/zratio_surfaces.R:411`) errors. Pre-existing in rc1; q=4+ fine. FIXED in brief 05 (`d6d804e2`) on the recommended shape: `zratio_anchor_grids_empty()` guards the build before the cache lookup → NULL surface, and the additive path serving instead is EXACT there (a mediating block excludes the toggled edge's endpoints, so at cap ≤ 3 every component is a single node or bridge, below `SurfaceFamily::size_min` — `zratio_engine.h:45-49`). Agent-added scope, endorsed on review: the verbose fence message gained an honest fourth branch (was falling through to "surface build failed", untrue); single caller passes `size`, default NA keeps API safe. q=2..5 end-to-end smoke + predicate/message unit tests. Merged `b04dbd06`. | report 03 §4.1; report 05 |
| F-056 | minor | fixed-verified | `bgmCompare()` reads `ncol(x)` on UNVALIDATED input: `R/bgmCompare.R:428-429` computes `num_variables = ncol(x)` and unpacks the difference prior *before* `bgm_spec()` runs `data_check(x, "x")` (`R/bgm_spec.R:409`). A vector `x` therefore yields `ncol(x)` = NULL, and `matrix(theta, nrow = NULL, ncol = NULL)` inside `validate_bernoulli_inclusion` (`R/validate_model.R:471`) fails with "non-numeric matrix extent" — an internal error where the shipped validator would have said "x must be a matrix or data.frame." `bgm()` is correct by construction: its edge prior is unpacked *inside* `bgm_spec` (`:527`), after the data check. Cross-path convention divergence — the resident defect class, hit by MM on his opening bgmCompare call. Fix: drop the two lines below the data check (or call `data_check()` at the top of `bgmCompare()`); regression test = vector `x` gives the clean message. FIXED (`1d53bf39`): `data_check(x)` runs before `ncol(x)`; a vector `y` gets the same treatment. Merged `4969f843`. | MM, brief 07 |
| F-057 | major | fixed-verified | Character or factor `group_indicator` CRASHES `bgmCompare()` with "'bin' must be numeric or a factor". `R/build_spec.R:439-442`: `group = group_indicator` inherits character storage, the recode loop writes integers into it (coerced straight back to character), and `tabulate(group)` then rejects it. The repair `as.integer(group)` exists but only at `:652`, far downstream of both `tabulate()` sites (the post-listwise recode, `~:505-507`, repeats the pattern). Factors are hit too: `as.vector(factor)` returns character. Not exotic — the package's OWN shipped `Boredom` dataset carries a character grouping column (`language`, "en"/"fr", 496/490), so the natural two-group call on it fails. Zero test coverage: every fixture hand-coerces (`helper-fixtures.R:216,328,351` `lang = as.integer(as.factor(...))`, comment "Convert language to integer: 1 for first level, 2 for second") — the workaround stands in for the missing test, which is why this survived to rc1. Fix: coerce once, `group = match(group_indicator, unique_g)`, which also deletes the loop; add character/factor fixtures. MM's report 07 independently confirms. FIXED (`fd191193`): `match()` at BOTH recode sites, first-appearance numbering preserved exactly; character/factor/integer/0-1 codings agree at spec level; the natural shipped-data call `bgmCompare(Boredom[,-1], group_indicator = Boredom$language)` is a slow-tier regression test; fixtures pass `language` directly. NOTE: Boredom is stored fr-first ⇒ fixture group numbering flipped to fr = 1 (consumers verified structural-only; ACCEPTED). Residue: one Rd line in `?bgmCompare` documenting first-appearance numbering (docs pass). Merged `4969f843`. Residue CLOSED (brief 12, `b6bb498c`, merged `f24ad3c8`): `?bgmCompare` documents first-appearance numbering, the x/y case, and label carry-through, with the fr-first Boredom example. | MM, brief 07; report 07; report 12 |
| F-058 | minor | fixed-verified (hazard recorded) | Every non-blank `.Rbuildignore` line — comments included — is compiled as a live PCRE by both `R CMD build` and roxygen2's file scan: an unbalanced parenthesis inside a comment kills `roxygenise()` with "missing closing parenthesis" (measured mid-batch when a draft split a comment across lines). The six shipped parenthesized comments happen to balance, so the hazard was latent; brief 05's hunk rewrote the touched comments paren-free. Standing rule → MAINTAINERS dragons: `.Rbuildignore` comments must stay regex-safe. | report 05 05-1 |
| F-059 | note | open (MM, feeds brief 08) | Post-#193 route geometry: the additive kernel's only serving band (shape < 0.5) coincides EXACTLY with the uncertified-constants band (certified range [0.5, 20]), so no certified cell can exercise the additive kernel end to end — the gauge's firing test necessarily runs on unscored constants (acceptable for a detector test; documented in-test). If MM wants an inside-envelope exercise it requires a deliberate test-only surface-withhold hook, which the agent rightly declined to add unbidden. Decide hook-or-accept during the zratio math read (brief 08). | report 05 05-2/OQ2 |
| F-060 | major | fixed-verified | bgmCompare `verdicts()` print, three defects from MM's pass: (a) the DEFAULT fit (`main_difference_selection = FALSE`) leaks 8 main-effect rows as `pip NaN / log_bf NA / verdict <NA>` interleaved with real rows, counted into "36 indicators" and "8 never updated" — mains are not under selection and should be excluded, with one honest line saying so (ties to F-029's compare-RB-NA note; with selection ON the table is correct — MM: "Yay!"); (b) the scale-contingency caveat the vignette promises for difference verdicts does not print; (c) footer tone. FIXED (`ded8418d`): printed counts/body exclude unselected mains via an `unselected_main` attribute while the RETURNED 36-row table is unchanged (downstream indexers keep working — good API discipline); one honest banner line; the scale-contingency caveat prints on every compare verdict table (vignette wording); footer "Consider a longer run."; attributes documented in `?verdicts`; snapshots. Merged `4969f843`. | MM, report 07 |
| F-061 | blocker-candidate (resolved: major) | fixed-verified | `prior_sensitivity_check()` on bgmCompare fits throws paired recycling warnings (`longer object length is not a multiple of shorter`) from `(p/(1-p))/prior_odds` — on BOTH the default and the mains-selected Boredom fits. Mechanism located by lead: `R/refit_engine.R:315-336` sizes `prior_odds` by `length(enm)` (indicator NAMES) while `pbar`/`pc` follow the RB draw layout; `R/prior_sensitivity.R:605/630` divide by the same vector. Recycled division ⇒ WRONG log-BFs on the difference path — same defect class as F-037. MM's curve anomaly (sit_around–half_dead_dull evidence INCREASING with difference scale, `reports/assets/sensitivity_compare.pdf`) is plausibly downstream; re-examine after fix. ALL bgmCompare sensitivity output is suspect until this closes — and F-049 may be a downstream casualty (same object pairs names-layout `edge` with draws-layout `pip`; see F-049's revised sequencing). RESOLVED (`7e31a491`, report 06) with the mechanism CORRECTED: the misalignment lived in the noise estimator `own_lbf`, which averaged the 76-column per-gated-parameter gamma matrix (28 pairwise + 8×6 thresholds) against 36 per-indicator prior odds — the exchangeable prior made every value individually right and the pooled MULTISET wrong (selected mains weighted ×6), corrupting the wobble q95, `$wobble$per_edge` (76 long vs 36 rows), and the mover-rule threshold; the curve, verdicts, and chosen-scale columns were always per-indicator and CORRECT. Boredom movers unchanged by dataset luck, not by construction. A silent even-division variant sat in the suite (22-vs-10 recycles warning-free); regression test pins warning + length. Single-group bgm: exact no-op (`anchor_draws` sets indicator = gamma, `R/anchor_curve.R:38-43`) — no released number affected. Refit cross-check 0.90/1.17 of refit noise. MM's rising edge (sit_around–half_dead_dull, 1.88→3.79 nats over 0.4×–2.5×) is REAL: the anchor fits' own exact RB verdicts show undecided→presence with no reweighting involved. Merged `4969f843`. | MM, report 07; lead trace |
| F-062 | major | fixed-verified | bgmCompare plots, MM's pass: (a) DECIDED — the difference network must encode Bayes factors/verdicts exactly as `bgm`'s network plot does ("For bgm it is BF, we should do that here too"); establish what it currently encodes, converge. (b) Legend overlaps the plot at defaults. (c) Wording: the "main-effect differences not selected" banner and the "node: main-effect difference" legend label read wrong. (d) MM proposal: when main selection is ON, draw main-difference evidence as qgraph-style node fill (`pie = pip`; 1 = full ring, 0.5 = half). (e) `type = "groups"` shares (b)/(c). RESOLVED (`1b99e4cd`): (a) established the difference network ALREADY encodes verdicts on bgm's convention — solid/dotted/absent is the BF-verdict channel, width is the posterior-mean difference magnitude — no change needed; (b) legend + subtitle in a reserved bottom strip (qgraph `mar`); (c) banner matches F-060's line; (d) IMPLEMENTED small via qgraph's `pie`: node rings filled by main-difference PIP, verdict-colored, legend "node ring: P(main-effect difference)". Renders: `assets/compare_plots_fixed.pdf`. Merged `4969f843`. | MM, report 07 |
| F-063 | minor | fixed-verified | Difference centrality is not interpretable — MM: "No idea what these mean. Lets not do centrality for differences." DONE (`b0398b11`): the difference display (per-draw between-group strength difference) is REFUSED with the reason — strength sums absolute weights, so a difference conflates which edges differ with sign cancellation (zero = identical networks OR compensating differences); per-group centrality kept; extractor/summary keep the difference path with the caveat in `?extract_centrality`. Merged `4969f843`. | MM, report 07 |
| F-064 | note | open (backlog/research) | The fragility flag on difference indicators correctly prints its not-validated disclaimer (UFC-2 was single-group ordinal only — no overclaim, Q2 answered clean). MM wants the validation EXTENDED to bgmCompare so the disclaimer can be dropped: post-release research item, alongside the full F-021 calibration method. | MM, report 07 |
| F-065 | minor | fixed-verified | bgmCompare startup: 5–10 s of complete silence before the progress bar appears (Boredom, p = 8, n = 986, defaults) — no message about what is running (spec build? correction table? warmup init?). DIAGNOSED + FIXED (`8019134e`): not initialization — spec build 0.1 s, step-size heuristic 4 ms; the progress manager rendered only every 50 main-thread iterations and the earliest (unadapted, deepest-tree) NUTS iterations are the slowest, measured 6.8 s to first render. Bar now renders at iteration 1; polling/throttle/interrupt cadences unchanged; `bgm()` gains the same immediacy. Merged `4969f843`. | MM, report 07 |
| F-066 | minor | decided (enhancement) | `plot_edge_posterior()` is below paper quality — MM (2026-08-01): "Our plot really is not THAT good"; target = the JASP prior-and-posterior figure (R Graph Compendium §prior-and-posterior). Redesign decided: conditional-on-inclusion posterior density + analytic slab-prior overlay, PIP probability wheel (one convention with F-062d's node rings), log BF + median/CI annotations, compendium axis conventions; the Savage-Dickey ordinate dots deliberately do NOT transfer (bgms BFs are RB indicator BFs, not density ratios at zero — dots would assert the wrong estimator). Decisive-absence edges get a figure instead of today's `stop()`. REFINED by MM: Savage-Dickey dots ARE drawn when `edge_selection = FALSE` — the SD ratio is the licensed estimator there (continuous posterior, no indicator; ordinates: analytic prior, SJ-bandwidth KDE at 0), printed as log BF with a BF/(1+BF) wheel; the no-dots rule applies only to selection-on fits (RB indicator BFs). Panel rule: show whichever estimator the model licenses. Brief 10 also extracts the reusable style module (`R/plot_style.R`: panel par, probability wheel, annotation block, no-big-titles convention) that F-067's package-wide sweep builds on. Sequenced AFTER brief 06 merges (same-file conflict, `R/plot_bgms.R`); MM reviews before/after renders pre-merge. IMPLEMENTED on `feat/edge-posterior-panel` (report 10, commits `a682b57e..8ca0f0f9`, gates green — suite 1187/0/0 both tiers, `--as-cran` 2 baseline NOTEs): `R/plot_style.R` style module (6 stated conventions) + the panel + the dots rule exactly as decided; decisive-absence `stop()` → figure ships; `binwidth` lifecycle-deprecated; `edge_panel_title()` retired. Statistical notes, LEAD-VERIFIED IN SOURCE: the prior overlay needs NO change of variable — slab frame = extractor frame on every family (GGM stores precision, extractor and `anchor_draws()` both apply −0.5, `ggm_model.cpp:310`; mixed STORES −Ω/2 directly, `mixed_mrf_metropolis.cpp:534`, both pass through; ordinal native). The agent first applied a ½ Jacobian, caught and removed it — had it shipped, GGM/mixed priors drew 2× too wide. Frame identity pinned ordinal-only → GGM fixture extension queued (brief 13). Slab ≠ marginal on continuous blocks documented + measured (PD restriction pulls the true marginal SD to 1.34 vs the slab's 2.0, p = 4, joint spec) — in the Rd. Wheel filled to RB pip (lead rec: KEEP — agrees with the printed log BF and `verdicts()`; empirical-share alternative recorded for MM). Lead accepted the deviations incl. the compendium's adaptive legend rule (negative weights make a pinned top-left legend overlap the curve). MM VERDICT (2026-08-01): approved with changes — MERGED `6ed36b44`. The changes, all brief-11 items: the verdict-word annotation goes ("undecided" / "evidence of absence" removed in every case — the figure shows evidence, `verdicts()` speaks verdicts), "PIP" becomes a self-explanatory inclusion label (lead proposal: "P(included)"), three decimals confirmed ("OK"), wheel stays RB ("RB indeed"). | MM, 2026-08-01; lead design; report 10 |
| F-067 | major | fixed on develop (merged `f3f18941`; NEWS `66f7d17f`; program closed) | Package-wide plot restyle to the JASP/compendium feel (MM, 2026-08-01: "could we use that style throughout the software? Making sure that all the plots have a similar feel"). One visual language via the brief-10 helpers: typography, offset axes, no box, NO big main titles (the sensitivity plot's headline title explicitly called out), wheels/rings carry probabilities, natural-log "log BF" text, muted captions. Sources: R Graph Compendium broadly + the JASP repo (`jaspGraphs` holds their working constants) — distill, don't port (ggplot2 vs our base graphics). Execution: brief 11 (after 10) restyles every remaining plot — network, compare group/difference panels, centrality survivors (F-063), sensitivity (after 06's nats conversion), calibration — and MM judges BEFORE/AFTER renders for EVERY figure pre-merge (standing requirement, at his request). DECISION OWNED: this deliberately deviates from the July feature-gap placement rule ("presentation goes downstream") — MM, 2026-08-01: "I am happy with the drift. I own it." bgms ships paper-grade DEFAULT presentation; easybgm keeps derived variants. Do not relitigate against the July doc. RENDER VERDICT ROUND 1 (MM, 2026-08-02, on brief 11's 21 pairs) — revision round issued on the same branch. LOCKED perfect: edge-panel presence/undecided/absence. RATIFIED: evidence-keyed legend (variant B) package-wide, thresholds in RAW BF units ("BF > 10", never "log BF > 2.3" — users don't think in logs; numeric texts stay natural-log); network EDGES ENCODE EVIDENCE, not model-averaged weights (the width-∝-weight encoding rejected on every network figure). CUT EVERYWHERE: the top-right evidence band (threshold/strongest-for/against block), all in-figure caption lines ("Width: …" → Rd), answer-sentence titles (sensitivity's "N verdicts depend on the scale" → print()), jargon in figures ("isotonic" kind subtitles; "H1"/"H0"/"data|H0" tags on the Savage-Dickey panel). REVERSED: report-10 deviation 8 / brief-11 task 6 — the beside-node probability_wheel is rejected ("Hate this"); restore qgraph-style rings AROUND node circles (the BEFORE is what MM wants; wheels remain only as standalone marks, e.g. the locked edge panel — lead's task-6 instruction induced this, owned). ALSO: legends too small (all networks); "Group 1 (1)" labeling unclear (vacuous parenthetical — group_tag fix); compare-groups unreadable + only-works-for-two-groups (K>2 layout required); sensitivity plots must CONNECT trajectories through the anchor dots (curve stubs + floating dots read as broken), "chosen" marker needs a self-evident name, off-range note moves to print(). CORRECTION (lead, 2026-08-02, after reading the docs-site MM cited): "edges reflect evidence" means the documented THREE-PANEL EDGE EVIDENCE PLOT — bgms-docs `guide/getting-started.qmd:79-132`, "the display this documentation uses for every network result": presence / absence / undecided panels on ONE shared layout (label + count as panel titles); ONLY the presence panel weighted (width = posterior mean association, "the effect sizes live here"); absence dashed uniform ("the classification, not a weight, is the result"); undecided dotted uniform; categories at inclusion BF 10 in either direction (raw-BF language is native to the docs). The lead's width-∝-capped-log-BF spec in the first revision block was a MISREAD — superseded by corrective addendum before implementation. MM (mid-turn): "The same holds for bgmCompare's plots" — the difference display is the same three panels for difference evidence. The one-panel legend A/B question largely dissolves (panel titles carry the categories). DESIGN CLOSED — MM's governing rule, verbatim (2026-08-02, authority: MM as PI; the docs site/tutorial are downstream records of his decisions, not authority over him): "We do not plot networks based on their edge weights, except when edge_selection / difference_selection is OFF. When edge_selection / difference_selection = ON, the edges in the network plot reflect the triaged edge evidence." No further litigation; remaining judgment happens on round-2 renders only. ROUND-2 VERDICT (MM, 2026-08-02 — six items, each package-wide for its element): (1) sign legends REMOVED everywhere ("positive"/"negative" are weight-talk; colour keeps the sign, Rd documents it) — this also resolves the compare-difference-no-selection legend-on-network overlap he flagged; (2) any surviving key text names evidence classes with their BF rule ("present (BF > 10)" / "absent (BF < 0.1)"), never signs; (3) panel titles ("evidence of presence/absence", "difference supported/ruled out", "undecided") BIGGER on every multi-panel display; (4) the "node ring: …" explainer line on the main-selection figure removed (Rd material); (5) edge-panel bottom caption ("Density: … Pale share of the wheel: …") removed on ALL FOUR variants — this UNLOCKS the round-1-locked trio BY MM'S OWN ORDER (the lock protected against agent drift, not against his refinement; trio re-renders with only this delta); (6) threshold rule lines under titles stand (unobjected). Round 3 = these six + the K=3 pair from the lead's K>2 ruling. ROUND-3 COMPLETE (2026-08-02, tip `ad648ca7`, six commits; gate 8258 pass / 0 fail / 0 warn / 105 skips; 25 render pairs with BEFOREs byte-identical; report on-branch + Dropbox copy hash-verified `2e094c36`; scope lead-verified: no forbidden paths, `verdicts.R` and `NEWS.md` untouched — the drafted NEWS clause lives in the report only, and its legend-removal half-sentence, agent-flagged, is DROPPED at integration under the never-shipped rule): K>2 landed per the lead ruling in its OWN commit (`fd7067ed`) with an honest K=3 fixture pair (supported pairs cross-checked against `verdicts()`, not read off the picture); five verdict items in (`8a91cd39`; `cex_panel_title` 1.7 with fit-to-width shrink; 7 caption-only snapshot deletions). Lead triage of the agent's own calls: `legend` argument REMOVAL RATIFIED — never shipped, the tag exports NO plot methods at all (lead-verified at `cran-0.1.6.3` NAMESPACE); the edge panel's Posterior/Prior curve labels RATIFIED as the sole key exemption (MM's rounds never flagged them; two curves, one axes pair); K>2 + `difference_selection = FALSE`: the accepted-error triage is SUPERSEDED by MM's design ruling (2026-08-02) — no error; the default plot dispatches to the GROUP PANELS itself (`compare_group_panels()`: each group's own posterior-mean weighted network, shared layout, paginated), the selection-off weights analogue of bgm's full network and squarely inside the governing rule's OFF exception; K=2 selection-off keeps the ratified Difference-weights network (its single contrast exists; K>2 has none, so it falls back to the groups themselves — asymmetry MM-owned); addendum LANDED (`b2616f48` dispatch + `d17d3304` report-hash fix; lead-verified: the stop() is gone, the dispatch is the literal type="groups" three lines with max_panels/page/qgraph passthrough, both roxygen paragraphs rewritten to state the behaviour positively, the expect_error tests became expect_invisible + four pins incl. a K=2 selection-off guard fixture, and the K=3 selection-off render pair landed — its BEFORE pane states the old refusal, which is the delta; agent proposes a NEWS clause for the new default in the report, lead lands NEWS at integration); DELTA VIEWER delivered to MM (`render-judgment-11-r3.html`: the 2 new K=3 pairs + the 15 figures changed since his round-2 verdict, judged-version vs now); MM RENDER VERDICT ROUND 3 (2026-08-02): "Figures all good." — the judgment half of the merge gate is SATISFIED, no further render rounds; merge-tree dry run vs develop `754e0388` CLEAN; FINAL GATE (tip `b9255b8f`): 8264 pass / 0 fail / 0 warn / 105 skips, no snapshot re-records (the dispatch reached no snapshotted panel), 26 render pairs, report re-copy hash-verified `1f34c9d1`; MERGED into develop `f3f18941` (--no-ff, clean); NEWS LANDED `66f7d17f`, parse gate 33 entries — round-2/3 feature clauses folded into the three plots bullets, the "remain easybgm's / deliberately not duplicated" sentence REMOVED (MM owns the drift), edge-posterior `data|H1`/`data|H0` and caption sentences patched, and NO Bug-fixes/Other-changes clauses landed from the program: 0.1.6.3 exports no plot functions, so every drafted change-clause (the legend sentence included) was never-shipped dev history, folded into feature text instead. PROGRAM CLOSED; the default-device three-panel squeeze = F-115 (→13). MERGE gated on MM's delta look once the suite lands and the report copies. | MM, 2026-08-01; MM rulings + round-2 verdict 2026-08-02; round-3 interim |
| F-068 | major | fixed-verified | `predict.bgmCompare()` centered every Blume-Capel term at baseline 0: `build_arguments_compare()` never stored `baseline_category`, so both the target's own quadratic term AND every neighbour's rest score were mis-centered — up to 0.23 category-probability error on a Boredom BC fit. Found by the F-021 calibration work (the new check caught it — the credibility argument for shipping it). FIXED `2934f3b4` (+ field whitelist `1508a888`) with a manual-reference test pinning the sampler's convention. Never released; rc1-era compare predictions with BC variables were wrong. Merged `4969f843`. | report 06 NEW-1 |
| F-069 | minor | decided | In-sample calibration of the Boredom compare fit (first output of the new bgmCompare check): several variables sit outside the 95% consistency band for up to ~30% of the curve (worst `entertain`, `loose_ends` in group 1, max_dev ≈ 0.17–0.19) while marginal category probabilities match to <0.01 — conditional-shape miscalibration, not marginal bias. Plausibly REAL pseudolikelihood misfit on real data rather than a machinery defect (margins + F-035-standard round-trips argue the code is right). MM DECIDED (2026-08-01): "this might be real misfit. Future work is geared towards resolving this misfit." Not a release gate; keep as the documented-example candidate. | report 06 NEW-3 |
| F-070 | note | fixed on develop (merged `f3f18941`, brief 11 — measured margin from the widest name, offset in inches; the render pair proves it) | Sensitivity plot right-margin edge labels can clip at the device edge at default width (pre-existing; visible in before AND after PDFs). Folded into brief 11's style sweep. | report 06 NEW-4 |
| F-071 | major (upgraded) | fixed-verified (merged, brief 12; T2 budget proof pending first Sunday on main) | The full suite no longer fits the nightly's CI budget: the 2026-08-01 `workflow_dispatch` run on develop was CANCELLED at exactly `timeout-minutes: 120` (17:01:41→19:01:57 UTC) with no test verdict. Pre-freeze scheduled runs finished in ~1h39-40m; the 0.2.0.0 suite (tests +25.8k lines) exceeds 2 h on GitHub's 2-core runners (locally ~12 min on 15 cores). CONSEQUENCE: Monday 2026-08-03's scheduled run on main will be CANCELLED too — it will show neither the 4 expected stale-fence failures nor any clean signal, and reads as infra failure. Remedies: bump `timeout-minutes` (≥240) and/or split fast/slow into separate jobs; note a develop-side fix does NOT rescue Monday (scheduled runs use MAIN's workflow file) — main is frozen, so either pre-announce Monday's cancellation or authorize a CI-only touch to main. Local signal is not degraded: the definitive suite state is report 06's branch-build gates (slow tier: 1 failure = F-049 only). MM DECIDED (2026-08-01): no timeout patch — "we specify and condense the nightly tests." Respec drafted by the lead on the 2026-07-29 tiering audit's bug-class principle (that audit's T0 ~90 s local split already shipped as #185 `c1e52c8e`): T1 nightly heartbeat = curated calibration subset, ≤ 60 min on 2-core CI, timeout 90, scheduled on DEVELOP (closes F-044's branch gap); T2 weekly certification = the heavy MC suites (SBC, recovery sweeps, n = 2e6 channels, refit cross-validations), Sunday, timeout 360. Brief 12 measures (harvesting real 2-core per-file timings from cancelled run 30709427193's log), classifies every slow block by bug class, implements the gates + workflows, and proves T1's budget with a dispatched run. STATUS (2026-08-01 relay): 98 blocks classified 46 T1 / 52 T2, none dropped; first T1 dispatch measured 80 min vs the 60-min spec — the nine zratio-* files the cancelled run NEVER REACHED were unmeasured (~35 min of hidden cost; the old nightly's true cost was never known, only its floor — a finding in itself). Re-cut of the zratio gold/oracle tail toward T2 in progress per the brief's conflict rule; lead guidance: T1 keeps at most a minutes-scale single-cell surface-vs-gold probe. SHIPPED (merged `f24ad3c8`): final split 38 T1 / 60 T2, none dropped; T0 push/PR fast gate (`fast-checks.yaml`, timeout 30); T1 daily 03:00 UTC on develop, PROVEN 43.1 min green (run 30735988783; four runs to land — 90.3 cancelled / 37.6 red on never-executed blocks / 59.8 green-but-12s-inside / 43.1; 36% runner variance on identical work, budgeted for); T2 Sunday 03:00 UTC, ~70 min measured blocks vs timeout 360 — UNPROVEN until the workflow reaches main (crons fire from the default branch; watch the first real Sunday after re-merge). Tier contract in MAINTAINERS §6; full 98-block class table in report 12. Equivocal calls RATIFIED by lead: bgmCompare full-defaults fit (828 s) and extrapolation size-cap fit (448 s) T2 on cost against class — first two back if the budget ever rises; `ggm-nuts` (12.7 s; its header claimed "several minutes", stale by two orders — corrected) kept T1 as the concordance smoke; hier-zratio ten-cell battery T2 as a condition grid per the contract line. | lead, CI run 30709427193; report 12; lead merge check 2026-08-02 |
| F-072 | minor | fixed-verified (merged, brief 12) | Group identity is opaque in bgmCompare output: the original labels are dropped at spec build (`unique_g` stays local to `build_spec_compare`) and every surface speaks bare integers — extractor colnames `group1/group2` (`extractor_functions.R:1145,1177`), centrality labels (`centrality.R:173,178`), plot titles "group %d" (`plot_bgms.R:509`), calibration panel titles (`calibration_check.R:828`), summary/print headers (`methods_bgmcompare.R:185,212`). MM (2026-08-01): make the label transparent. Fix shape, respecting the extraction contract: STORE `group_labels` in arguments at build (the F-068 `baseline_category` precedent — one line + whitelist); KEEP machine-facing extractor colnames stable (easybgm/JASP consume them — F-002/F-029); surface labels on the human displays — a mapping line in print/summary ("groups: 1 = en (n = 496), 2 = fr (n = 490)"), plot/calibration titles "group 1 (en)", centrality labels likewise; x/y-path fits label as "x"/"y". FIXED (`f4c683a2`, merged `f24ad3c8`): `group_labels` stored at spec build (first-appearance order; x/y path; listwise-renumber carries surviving labels), surfaced via `compare_group_labels()`/`group_tag()` on print/summary headers, plot titles, calibration panels, centrality labels; extractor colnames stay numeric per the easybgm/JASP contract; pre-label fits degrade to bare numbers. Lead-verified in diff. NOTE: the brief's illustrative mapping line had the groups swapped — Boredom is fr-first, so the shipped line reads "groups: 1 = fr (n = 490), 2 = en (n = 496)" (report 12 finding 6; consistent with F-057's fixture flip). | MM, 2026-08-01; lead recon; report 12 |
| F-073 | major (test coverage) | fixed-verified (merged `8bfa6148`, `b5c6d85e` + `ce4ebe7f` — all four holes closed: planted-δ scale pin proven to bite, two-sided bounds, group 2 asserted, simulate convention test; the diagnosis deepened en route: the OLD guard was passing BY SEED, its 0.5 factor met by only 8/12 alternative seeds — the new derived tolerances replace that foundation) | The every-run compare convention guard (`test-bgmCompare.R:337-368`, review law #1's front line) has directional and surface holes — lead-verified against the source: (1) LARGEST — no every-run test pins the scale of a NONZERO group difference (the guard's two groups share one omega, `difference_selection = FALSE`), so a ×2 introduced in the difference parameterisation alone — precisely the surface the 0.2.0.0 breaking change touched — passes the entire every-run tier; (2) the guard is one-sided: both assertions pass a ×½ divergence with room to spare (rmse 0.143 vs 0.2; 0.265 vs 0.291 — `0.5·rmse(2·target)` only ever tests the doubling direction); (3) group 2 is never asserted (`pairwise_effects_groups[, 1]` only; observed group 2 visibly worse and unchecked); (4) `simulate.bgmCompare()` has NO numeric-convention test — coverage is input validation + structural assertions only, the one group-difference test self-describes as "a soft test" (`test-methods.R:730`); report 09 item 5 filled the gap empirically (simulated margins vs predict expectations agree to 0.0083 at nsim = 20000) but no test pins it. All four closures are cheap; (1) alone needs a small design (planted δ, second omega, ~5 s budget — reuse the guard's own construction). Main effects also unpinned (lesser). | report 09 §09-4; lead verified 2026-08-01 |
| F-074 | minor | resolved (2026-08-03 — collapse contamination, proven by 19's re-measure; MM ratification window open) | bgmCompare's per-group pairwise LEVEL runs 6–9% above matched `bgm()` fits of the same rows — while the SCALE is clean (09-1: contrast slope 1.014 [0.994, 1.034]). Group slopes 1.026/1.100, level 1.064, max\|Δ\| up to 0.082 against a derived 0.0063 refit-noise tolerance. Pooling REFUTED by the agent's control: compare-vs-full-data-bgm slope 1.088 vs the half-fits' own 1.022 — the compare level is MORE extreme than both the half and full fits, not between them. Specific to `difference_selection = FALSE` (selection on: level slope 0.996). Changes no verdict at observed magnitudes. Lead hypothesis for MM's math eye: induced-prior geometry — the group value = baseline + δ/2 sums two slabs, so its induced prior is wider than bgm()'s single slab ⇒ less shrinkage in the level; the contrast, governed by the δ slab alone, matches. Cheap discriminator queued for brief 13: shrink `difference_scale` toward 0 — the level should collapse onto the pooled fit if prior geometry, and not if sampler. PHASE A LINK (16, 2026-08-02): the ten-seed planted-difference batch shows a systematic ~4–5% slope offset on the 36 healthy edges in EVERY seed (compare 1.043/1.027 vs bgm 0.986/0.991 after dropping the degenerate variable's edges) — the agent identifies this with F-074's geometry. TENSION TO RECONCILE: 09-2 measured the offset only at `difference_selection = FALSE` (selection ON was clean, level 0.996), while Phase A's design runs selection on — the reconciliation (design difference or 09-2 scoping error) is owed in 16's report. The discriminator is NOW RUNNING as 16 Phase B's `difference_scale` sweep (healthy-edge curve tracked separately from the runaway curve) — brief 13 should NOT rerun it, only consume the result. PHASE B INTERIM (2026-08-02): the agent WITHDRAWS the induced-C(0,1.5) explanation, on its own 09-2-metric re-analysis — the healthy-edge level offset is 4.3% even on edges whose difference indicator is ZERO, where the group value is the baseline alone under C(0,1), IDENTICAL to bgm()'s prior (no induced-prior difference exists there to explain anything); indicator-included healthy edges sit at 1.135, avoidact at 1.313. New reading: CONTAMINATION — the avoidact runaway drags the shared solution (via shared/coupled main effects), lifting healthy edges ~4%; this also RECONCILES 09-2 without contradiction (its clean 0.996 came from a real-data null split with NO degenerate variable — nothing to contaminate). Sweep curves flat in `difference_scale` (1 → 0.5) — consistent with contamination, against any prior lever. Discriminators queued in 16: the null-split-in-degenerate-regime cell and the δ dose–response arm (offset tracking the PRESENCE of degeneracy confirms contamination). F-074 stays OPEN pending those; if contamination confirms, F-074 likely COLLAPSES INTO F-075 (one defect, two shadows — direct runaway on degenerate edges, indirect drag on healthy ones) and closes with F-075's fix. FINAL BRIEF-16 STATE (report 16 §4): OPEN, contamination the lead hypothesis, the two confirming cells (null-split-in-degenerate-regime; dose–response) CUT once the F-075 mechanism was found deterministically — correct prioritization, lead-endorsed. Resolution path: brief 19's post-fix re-measure — if the healthy-edge offset vanishes on fixed code, the 09-3-side F-074 was collapse contamination and closes with F-075; the 09-2-side (6–9% at selection-off on REAL shared-support data) then needs its own one-cell check before F-074 fully closes. SETTLED (brief 19 §4.3): on the SAME seeds and metric the healthy-edge offset falls from ~+4.3% to +0.8%/+0.5% and both group slopes land on bgm()'s — report 16 §4's contamination hypothesis confirmed; the planned one-cell check is moot; no real-data claim was ever in this row (09's matched-n ran on simulated rows). | report 09 §09-2, §3.3; brief-16 report §4 2026-08-02; report 19 §4.3 |
| F-075 | **blocker** (upgraded by MM) | FIXED-VERIFIED — RELEASE GATE CLEARED (merged `02c609c3`, brief 19) | The MATCHED-N apples control settles 09-3 against bgmCompare: on byte-identical simulated data at n = 2000/group, two separate `bgm()` fits get group slopes vs planted truth 0.963 [0.921, 1.005] / 1.034 [0.931, 1.136] (group-2 rmse 0.051; true-zero-difference noise sd 0.041, max 0.110) where the ONE compare fit gets 1.270 [1.159, 1.382] / **1.926 [1.599, 2.253]** (rmse 0.214; noise sd 0.156, max 0.830) — non-overlapping intervals on group 2, 3.8× the noise, 4.2× the rmse. FIVE alternative explanations tested and refuted along the way (simulation mixing, round-trip fidelity, regime conditioning, truth recoverability, units), the agent's own two wrong turns recorded in the report rather than hidden. Detection was never in question (δ = 0.40 the only presence, PIP 1.000; null splits near-nominal; scale convention clean per 09-1). TWO QUALIFICATIONS (agent's, lead-endorsed): (1) CONTRAST inflation is partly shared small-sample behaviour — `bgm()`'s own contrast slope at this n is 1.500 [1.314, 1.687] (0.883 at n = 4000); only the group-level slopes and the noise inflation are compare-specific, and the earlier "3×" framing against the n = 4000 control overstated. (2) One dataset per cell, and the group-1/2 asymmetry flips between n-cells — no fix gets scoped off a single seed. THE PROGRAM (agent's order, = brief 13 scope): (i) ten data seeds at n = 2000/group — observation → operating characteristic, THE gate; (ii) `difference_scale` sweep — inflation shrinking toward the realistic ~0.1 scale makes the DEFAULT the lever, not the sampler; (iii) does it reach REAL data — item 1 says not at n = 172/group on Wenchuan splits (difference-mean sd 0.046–0.058); whether an n / effect-size regime hits real analyses decides release impact. Lead lean (characterize + document + chase post-release) OVERRULED by MM (2026-08-01): "we need to clear this up and fix this for the release" — F-075 GATES SUBMISSION. Brief 16 runs the program as the authorized heavy lane; the fix itself lands as a separately reviewed batch. PHASE 0 COMPLETE (2026-08-01 interim): (1) MIXING REFUTED — 09's cell reproduced to 3 decimals on a fresh export; the runaway parameters are the BEST-mixed (avoidth-avoidact baseline 1.338 ± 0.138, Rhat 1.006, four chains at 1.32–1.35 vs bgm 0.602/0.613, truth 0.631) — a sharp posterior 5.1 SDs from truth agreed by every chain, not an excursion. (2) LOCALIZED — residual sd 0.336 on avoidact edges vs 0.024 on the other 36; dropping the top-3 residual edges collapses the group-2 slope 1.926 → 1.037; the n=400/2000 "flip" is just which group's avoidact edges ran away. (3) THE REGIME — the planted truth is high-gain: OM ± δ/2 moves the network grand mean 1.77 → 1.02 (g1) / 3.47 (g2), and group-2 avoidact lands 94.6% in ONE category (3 obs below cat 3) — near-degenerate data, i.e. quasi-separation; simulator re-validated (not an artifact). (4) TWO LIVE HYPOTHESES: (a) induced-prior geometry — compare's induced prior on a group value is Cauchy(0, 1 + difference_scale/2) = C(0, 1.5) vs bgm's C(0,1), and the JOINT tail along the both-groups-large diagonal falls as 1/t² vs two independent fits' 1/t⁴ — capable of 5-SD effects precisely where quasi-separation flattens the likelihood (this is F-074's lead-hypothesized geometry, one mechanism two regimes); vs (b) a likelihood-side defect. Lead-approved grid ADDITIONS (beyond brief): δ dose–response arm; null-split in the degenerate regime; profile log-pseudolikelihood test — the DECISIVE discriminator (compare fits data worse ⇒ defect; equally well ⇒ prior-tail dominance on a ridge ⇒ fix moves to prior geometry + degeneracy diagnostics). PHASE A COMPLETE (2026-08-02, 10 seeds, none omitted): compare g1 slope 1.222 (sd 0.109) [1.112, 1.418], g2 1.148 (0.281) [0.953, 1.926]; bgm() g1 0.993 (0.025) [0.940, 1.020], g2 1.003 (0.064) [0.876, 1.101]. THREE CORRECTIONS TO 09's CHARACTERIZATION: (1) the inflation is SYSTEMATIC and sits in GROUP 1 — outside bgm's whole range in every seed, worse group = g1 in 8/10; 09's asymmetry-as-random-flip was wrong, and seed 1's g2 = 1.926 was a genuine tail draw (other nine in [0.95, 1.13]). (2) The "3.8× noisier" claim DOES NOT SURVIVE: true-zero-difference noise ratio mean 1.11, range [0.15, 2.68], compare QUIETER than two bgm() fits in 5/10 seeds — 09's 3.8× was seed 1. (3) CLEAN DECOMPOSITION: dropping the 9 avoidact edges collapses compare slopes to 1.043/1.027 vs bgm 0.986/0.991 in EVERY seed — the finding = a small systematic ~4–5% offset on the 36 healthy edges (agent identifies it with F-074, consistent with the C(0,1.5) induced tail; reconciliation with 09-2's clean selection-ON level 0.996 owed in the report) PLUS a large intermittent runaway CONFINED to the degenerate variable's 9 edges (the F-075 core). (4) DETECTION UNHARMED: δ = 0.40 found 10/10; false presences 14/410 = 3.4%, inside the ≤10% BF budget — the harm is estimation-scale, not detection. DESIGN FLAW (agent-flagged, lead-endorsed): 3 of 09's 4 planted edges touch avoidact, and planting +δ/2 there is what drives it to 94.6% one-category — the construction manufactures its own degeneracy on the variable it measures, so 09's headline recovery (compare 1.59× truth vs bgm 1.10×, an avoidact-lossint edge) overstates by construction. This does NOT dissolve the gate: near-constant variables are ordinary in real data and quasi-separation is reachable without planting — but the eventual finding/NEWS framing must quote the DECOMPOSED numbers, never the seed-1 aggregates. PHASE B RUNNING: difference_scale sweep on seed 1, second sweep on seed 8 queued (largest g1 inflation 1.418 + a 0.54 null excursion) so the lever isn't scoped off one extreme cell. Lead steering at Phase A acceptance: per scale value track healthy-edge and avoidact-edge curves SEPARATELY (the sweep discriminates F-074 and F-075 jointly); the profile log-pseudolikelihood test remains the decisive mechanism call on the runaway cells; free addendum from existing draws — per-edge recovery for the ONE planted edge not touching avoidact across all ten seeds (completes "recovery is clean off-degeneracy" at zero fits). PHASE B INTERIM (2026-08-02, all four steering items answered from existing draws): (1) THE PROFILE LOG-PSEUDOLIKELIHOOD TEST FIRED — DEFECT, NOT PRIOR. On every runaway cell the compare solution fits the data WORSE than bgm()'s, each at its own best main effects: seed 1 scale 1 dLL −492.3 total (−0.123/obs), seed 8 −319.4 (−0.080/obs), seed 1 scale 0.5 −512.8 (−0.128/obs); seed 8's deficit sits in g1 exactly where its inflation is (avoidact g1 1.865 vs g2 0.997) — the deficit TRACKS the runaway. A deficit of hundreds of nats cannot be bought by any prior tail: the sampler's effective objective diverges from the evaluated pseudolikelihood ⇒ likelihood-side defect. (2) BOTH SWEEP CURVES FLAT scale 1 → 0.5 (healthy g1 1.0811 → 1.0816; avoidact g2 2.7913 → 2.7822) — independent confirmation the prior scale is NOT the lever; scales 0.25/0.1 now confirmatory only. (3) The clean planted edge (intrusion-upset, δ = 0.05): GROUP-level recovery clean (compare 0.096/0.117 vs truths 0.084/0.134, ≈ bgm); the difference-level 0.41× is selection correctly shrinking a sub-noise-floor effect (δ below the 0.048 estimator floor; compare ran difference_selection = TRUE vs bgm edge_selection = FALSE — not like-for-like); Phase B selF cells settle it. FIX SPACE REDIRECTED: no prior re-spec, no default-scale change — a code defect to LOCALIZE. Lead steering at interim: (i) FIRST, zero-fit mechanism check from stored draws — state the exact main-effect-difference configuration of these fits (selected / free / constrained-equal?) and pull avoidact's main-effect-difference posteriors (indicators + values) against the two bgm() fits' main-effect gap; if compare's avoidact main differences sit off/near-zero where the per-group gap is huge, the pairwise runaway is COMPENSATION for constrained mains under quasi-separation — one mechanism that would unify fits-worse-at-free-mains, scale-flatness, healthy-edge contamination via shared mains, 09-2's cleanliness, and unharmed detection; (ii) Phase C = minimal reproducer + independent reference: tiny degenerate dataset, compare's acceptance-relevant logp differential vs an R-side reference, plus a category-recoding/support audit compare-vs-per-group-bgm on the same split. ROOT CAUSE FOUND (report 16, 2026-08-02) EXACTLY THERE: `collapse_categories_across_groups()` (`R/validate_data.R:407-417`) keeps only categories observed in EVERY group and folds the rest DOWNWARD, SILENTLY — groups differing in location (the ordinary group-comparison case) lose well-observed categories. On the F-075 data avoidact went 5 levels to BINARY (97.7% zero in g1 / 99.85% one in g2 — nearly the group indicator itself; g1 loses 3 of 4 observed levels vs g2 1 of 3, explaining the systematic g1 inflation); 7 of 10 variables lost at least one level. LEAD-VERIFIED INDEPENDENTLY: function read; reproducer re-run from scratch (4 categories x 40 obs each, group supports {0,1,2} vs {1,2,3} — collapses to binary, zero warnings); Boredom checked — does NOT collapse (language groups share support; the defect needs location-shifted groups, e.g. clinical-vs-control). NOT sampler, NOT prior (C(0,1.5) vs C(0,1) worth ~1 log unit against a 158-unit profile deficit), NOT difference_scale, NOT adaptation, NOT main-difference constraint (helper holds mains FREE at indicator 1 — lead-verified) — every knob inert because the defect is upstream of all of them. Agent CORRECTION accepted: the profile log-PL deficit measures DAMAGE on the user's data (reference used original codes, fit used collapsed), not sampler mistargeting — the sampler correctly targets the posterior of a mis-prepared dataset. EXPOSURE RESTATED: by slope 1/10 seeds, by max null-difference excursion 3/10 (up to 0.68 where bgm never exceeds 0.18) — the user-facing risk is a spuriously large individual difference. Detection intact (10/10, FP 3.4%). Operating-characteristics table in report 16 §5 describes DEFECTIVE behaviour; re-measure post-fix. TAG CHECK (lead, 2026-08-02): the defect SHIPPED — `cran-0.1.6.3:R/data_utils.R:388-394` carries the identical intersection loop ("Collapse categories when not observed in one or more groups"), so the fix is a TRUE 0.1.6.3-reachable bug fix ⇒ NEWS Bug-fixes entry with refit guidance (every released bgmCompare analysis on location-shifted groups was affected). MM FIX DECISIONS (2026-08-02): (1) UNION semantics; (2) BC exemption stays, DELIBERATE — BC scores are meaningful, rationale goes in the Rd; (3) F-112 hook in the fix batch. Brief 19 issued. GATE CLEARED (2026-08-03): union semantics merged `02c609c3` (fix `c3b315a6`); re-measure 10/10 seeds on the SAME truth/baselines — group slopes 0.998/1.012 (sd 0.029/0.033) vs bgm()'s 0.993/1.003, noise sd 0.019, max null-difference error 0.159 (was up to 0.68), δ=0.40 recovery 1.10× exactly matching bgm (was 1.59×), detection 10/10, false presences 2.2% (was 3.4%) — nothing worse than report 16's table anywhere; agent gates: suite 8320 default / 8524 slow, 0F/0W both tiers; `--as-cran` 2 baseline NOTEs; the reproducer BITES on an unfixed export (34F/5E; `test-collapse-categories.R:335`). LEAD verification: fresh reproducer re-run on the fixed build (union keeps 4 levels, `num_categories` 3, classed warning with correct per-cell text, support-matrix zero cells right, true gap still closes, BC untouched — ALL PASS) + post-merge cross-batch filtered suite on the merged tree (bgmCompare, gradient hook, collapse-categories, build-arguments, extractor-functions ALL PASS — the 19×20 argument-surface interaction is clean). Residual watch, post-release (report 19 OQ4/OQ5): K>2 structural-zero exposure is covered mechanically but not estimate-wise; heavy-tailed prior-pinned threshold differences may eventually cost NUTS tree depth (one energy advisory; min ESS 76 on seed 8). | report 09 §09-3; MM ruling; brief-16 Phases 0/A/B 2026-08-02 |
| F-076 | note | recorded (not a gate) | Null-split difference false positives are rare but the tail is STRONG: presence rates 1.5–2.9% of 136 (inside the ≤ 10% Bayes-factor-bound budget), yet split 103's `sleep-startle` hit PIP 0.9999998 = log BF 15.4, NON-fragile, on data with no true difference (9/408 null edge-fits crossed threshold; none repeats across splits, cross-split PIP correlations 0.009–0.037 — nothing structural). Operating-characteristic note tied to the `difference_scale` calibration caveat `verdicts()` prints. Proper characterization = `dev/plans/backlog/future_tasks.md` item 41 (fragility-flag validation — the same runs answer both), post-release. | report 09 §09-5 |
| F-077 | note | open (declined in 13 with recommendation: provenance runs through `R/validate_data.R` and the C++ readers take a bare arma::ivec with NO NA handling — an NA store would crash them; disposition sweep decides accept-vs-rework) | `arguments$baseline_category` stores a meaningless raw value for ordinal entries on mixed compare fits (`1 1 2 2 2 2` for BC/BC/ord×4 with `baseline_category = 2`: BC entries correctly shifted to 0-based, ordinal entries carry the raw input). Inert today — `src/mrf_prediction.cpp:99-105` reads it only inside the blume-capel branch, and predict matches a manual reference to 3e-16 — but a live trap for anyone writing a reference implementation against `extract_arguments()`. Candidate fix: store NA for non-BC entries; check the C++ read sites tolerate NA first. | report 09 §09-6 |
| F-080 | major | open — cores RULED OUT (brief 13: bit-identical draws at 2 vs 15 cores, margins 20–78×); remaining suspects BLAS/runner image; proof from post-merge CI nightlies | CI-only red, previously unowned: `test-mixed-nuts.R` block M.2F FAILED on the 2-core CI nightly (cancelled run 30709427193) while the same suite is green on 15-core local runs (reports 06/10 gates). Surfaced by brief 12's log harvest — the cancelled run had THREE failures, not the one (F-049) local runs show. The block is T2 under the respec, so T1 stays green, but Sunday certification will carry this red until owned. Suspected class: core-count/scheduler-dependent chain trajectories (the documented oneTBB ≥ 2022 behavior — chains beyond the first follow different trajectories across core counts) meeting a seed-tuned statistical tolerance. Brief 13: reproduce locally at 2 cores, decide tolerance-vs-real-difference, fix or re-found. | brief-12 status relay 2026-08-01 |
| F-081 | major | open — cores RULED OUT (bit-identical 2 vs 15); PLAUSIBLE MECHANISM FOUND: `test-sbc-ggm.R` drew data via MASS::mvrnorm whose eigen() is LAPACK/BLAS-dependent, silently re-randomising every "seeded" SBC realisation per platform — switched to chol() (`9f3101bb`, preventive, not proven causal); if the next CI nightly greens, this WAS the mechanism | Same class as F-080: `test-sbc-ggm.R` diagonal-ranks block FAILED on the 2-core CI nightly, green on 15-core local runs. Rank-based SBC assertions are exactly the shape a scheduler-dependent trajectory change would perturb. Same ownership path as F-080 (reproduce at 2 cores, diagnose, fix or re-found); the two should be diagnosed together — one mechanism would explain both. | brief-12 status relay 2026-08-01 |
| F-082 | minor | fixed-verified (merged, brief 14; = report 14's "F-100") | FALSE pre-existing NEWS claim: "Default `pairwise_scale` changed from 2.5 to 1". `pairwise_scale` is a bare deprecated formal with NO default (`R/bgm.R:501`), translated to `cauchy_prior(scale = )` (`R/bgm.R:561-569`, guarded on the user not having set `interaction_prior`) — so `bgm(pairwise_scale = 1)` does NOT reproduce the 0.2.0.0 default (`normal_prior(1)`): the deprecated path keeps the Cauchy FAMILY. Lead-verified in source. Replaced with an accurate default-change entry + a separate deprecation-path bullet. MM CONFIRMED intended (2026-08-01, Q4): "We introduced prior classes and changed the default family" — the deprecated formal keeps its original Cauchy meaning; the default-family change is a separate, deliberate decision. NEWS text stands. | report 14 F-100; MM Q4 |
| F-083 | minor | fixed-verified (merged, brief 14; = report 14's "F-101") | The pre-redesign `plot_edge_posterior()` description (stem at zero, probability per bin) survived in the same NEWS file as the brief-10 redesign entry — one released file describing one function two contradictory ways, the old one false at HEAD. Removed; the shipped panel is described once. | report 14 F-101 |
| F-084 | minor | fixed-verified (merged, brief 14; = report 14's "F-102") | NEWS presented missing-data imputation as new in 0.2.0.0; `na_action = "impute"` is a 0.1.6.3 formal (lead-verified at the tag) and was announced in this same file's 0.1.1 section. Restated as: coverage now extends to the new model classes. | report 14 F-102 |
| F-085 | minor | fixed-verified (merged, brief 14; = report 14's "F-103") | The last log10 residue after F-039: the `verdicts()` NEWS entry quoted the calibration distance as "0.25 on the log10 Bayes factor scale" while `R/verdicts.R:286` and `man/verdicts.Rd:57` both state 0.58 nats and contain no `log10`. NEWS restated to 0.58 on the (natural) log scale, matching the shipped Rd. Brief 15's sweep guards against any further residue elsewhere (its D-12 re-found this same site on a pre-merge export — resolved by this row). | report 14 F-103 |
| F-086 | major | fixed-verified (merged, brief 17) | The prior-sensitivity vignette's pasted `ps` report block (`vignettes/prior-sensitivity.Rmd:62-103`) is a hand-written MOCK that has never matched the shipped print method — both were introduced by the same commit (`4a7212fd`, #183; pickaxe-verified). Four divergences: "not certifiable (chains disagree)" vs shipped "(too noisy to assess)"; "cannot be certified" vs "are too noisy to assess"; a missing cause/remedy sentence the method prints; a "Details:" paragraph where the real footer is `Method:`/`Refits:`/`Noise:`. LEAD DECISION (report 15 Q1): fix by making the chunk LIVE — agent-measured ~33 s, inside the vignette budget — so it cannot drift again; MM may overrule to regenerate-and-annotate. SHIPPED live at 56 s (17 lengthened refits to clear the 1.6x anchor's convergence gate; report 17 finding 6). FOLLOW-UP DECIDED (MM, 2026-08-02): adopt the docs-site bake pattern for the package vignette — the site's `bgms-docs/fixtures/bake.R` + `ps_wenchuan.rds` (89 KB — the ps object is small; the FIT fixtures at 26–133 MB stay website-only) prove the shape: reader sees the real seeded call (`eval = FALSE`), a baked `ps` object loads hidden, and print/plot run LIVE at build so the F-086 drift-proofing is preserved (presentation cannot drift; schema drift fails the build loudly). Bake script committed + documented with a rebake-at-release rule; expected 56 s → ~10 s. Brief 13, after 11 merges (enables `plot(ps)`/`head(ps$edges)` chunks on the restyled plot in the same pass). | report 15 D-1, §4.1; report 17; MM 2026-08-02 |
| F-087 | major | fixed-verified (merged, brief 17) | INVERTED Bayes-factor reading in the diagnostics vignette (`diagnostics.Rmd:146,152-153`): the chunk prints BF_10 = 24.59 — strong evidence FOR inclusion (`verdicts()`: presence, log BF 3.20) — but the prose calls it "small … little evidence for inclusion" and reads 1/BF = 0.041 as "strong evidence for the absence". The vignette's ONLY worked BF example teaches users to invert evidence. Likely a Cauchy→`normal_prior(1)` default-change casualty (the prose was never re-read). Out-of-repo copies (tutorial, docs site) must be grepped for the copied paragraph — flagged in `plans/software/flagged-issues.md` item 19. | report 15 D-2, §4.2 |
| F-088 | minor | fixed-verified (merged, brief 17) | `prior-sensitivity.Rmd:143` still reads `\|\Delta log_{10} BF\|` — the one site the F-039 nats conversion (`4cc8ce41`) missed; both neighbouring sentences were converted. | report 15 D-3 |
| F-089 | minor | fixed-verified (merged, brief 17) | `diagnostics.Rmd:58` names four transition counts (`n0->0`,`n0->1`,`n1->0`,`n1->1`) "reported in the indicator summary"; the summary carries only `n0->1`/`n1->0` (`R/mcmc_summary.R:279-284`). | report 15 D-4 |
| F-090 | minor | fixed-verified (merged, brief 17; via the F-086 live chunk) | `prior-sensitivity.Rmd:144-145,169-170` names report elements that do not exist: "the report's Details line" (no such line) and 'prints these as "not certifiable"' (it prints "are too noisy to assess"). | report 15 D-5 |
| F-091 | minor (code) | fixed-verified (merged, brief 17; + pluralization and %g ride-alongs) | `print.bgms_verdicts()` ERRORS on a column subset that keeps all five display columns: `subset(v, fragile)` → `Error in log(threshold)`. `[.data.frame` drops the `evidence_threshold` ATTRIBUTE whenever `j` is supplied; the guard at `R/verdicts.R:454` tests for missing columns only. `?verdicts`' own example takes the safe row-only path, which is why it survived. LEAD DECISION (report 15 Q3): release fix = guard the attribute too (graceful plain-frame fallback); an attribute-preserving `[.bgms_verdicts` method goes to the backlog. Ride-along: the print's "(1 indicators)" grammar and `%g` Bayes-factor formatting. | report 15 D-6, §4.3 |
| F-092 | minor | fixed-verified (merged, brief 17) | `diagnostics.Rmd:84-86` claims RB columns are NA "only" for draws constant to double precision — too narrow: a second route exists (variance below the autocovariance kernel's numerical floor, `R/mcmc_summary.R:263-268`; observed: `intrusion-flash` sd 2.2e-11 with NA mcse/n_eff and finite Rhat). | report 15 D-7 |
| F-093 | minor (code) | fixed-verified (merged, brief 17; + the two source comments the messages came from) | Two shipped runtime messages (`R/zratio_surfaces.R:545-546`, `R/zratio_gauge.R:394`) claim 0.00028 nats is "two orders below" 0.003 nats — it is 10.7×, one order; the diagnostics vignette's "roughly a tenth" (`:237`) is correct and disagrees with the runtime. Fix the messages, not the vignette. | report 15 D-8 |
| F-094 | note | fixed-verified (merged, brief 17) | `diagnostics.Rmd:64-66` justifies full-chain ESS validity for a quantity the package deliberately does NOT compute that way — the reported `n_eff` is the composite RB estimator; `R/mcmc_summary.R:334-336` explicitly rejects the raw-chain route ("inflates in inclusion-dominated cells"). `mean`/`sd`/`Rhat` are full-chain, so the sentence is half-right; reword. | report 15 D-9 |
| F-095 | note (code) | fixed-verified (merged, brief 17) | The pooled transition scan carries `prev` across chain boundaries (`src/mcmc_diagnostics.cpp:352-371`: `int start = (c == 0) ? 1 : 0;`), fabricating `nchains − 1` transitions per parameter — can turn an exactly-zero `n0->1` into 1 (the precise corner the vignette teaches users to read) and biases legacy `n_eff_mixt` upward for barely-flipping indicators. Two-line fix (reset `prev` per chain). LEAD DECISION (report 15 Q4): in the 17 batch — cheap, real, user-facing column; re-record any snapshots it touches. | report 15 D-10, §5 |
| F-096 | major | fixed-verified (merged, brief 17) | MM's NEWS ratification read (2026-08-01): the merged 0.2.0.0 section still tells DEVELOPMENT history in places. LEAD-VERIFIED AT THE TAG: at `cran-0.1.6.3`, `bgm()` AND `bgmCompare()` BOTH use the old scale with no factor 2 (compare `bgmCompare_sampler.cpp:118-139`, bgm `bgm_logp_and_grad.cpp:178`, simulator `mrf_simulation.cpp:98`) â the released pair was MUTUALLY CONSISTENT, so the B6 narrative ("compare put omegaÂ·x where every other path puts 2Â·omegaÂ·x â compare â 2Ã bgm") describes a mid-development window, never a release; B7's "predict/simulate on compare were wrong" fixes a defect no released user could hit. AMENDMENTS (task 14): B6 â both-moved-together framing (compare values ~half of 0.1.6.3, refit-not-rescale kept); B7 â dropped; never-shipped rule applied UNIFORMLY to Bug fixes (mixed/GGM fixes drop; category-recode/imputation-cache/Alpine verified at the tag; the two NUTS fixes may be genuine 0.1.6.3 fixes â NUTS existed there); hamiltonian-mc bullet + GGM-gibbs cross-mention; standardize bullet + MM's g-prior rationale; compare RB-NA bullet + the proposed-vs-never-proposed clarification (MM's question, answered: NA â zero sampler visits, `extractor_functions.R:342`; bgm's always-1 indicators are still proposed each iteration hence finite). MM recollection corrected by the same verification: `simulate_mrf` WAS exported at 0.1.6.3, so the F-023 bullet stands as merged. EXECUTED (brief 17 task 14, merged ec1c91a4): amendments (a)-(g) all landed; per-entry tag-verdict table in report 17 §3.9. Never-shipped drops: mixed-PIP order, cross-indicator asymmetry, delta=NULL, Cholesky downdate (features absent at tag) PLUS two the brief said verify-first — category-scale recode (tag round trip self-consistent via min-shift; the quoted warning string absent from the tag) and both imputation-cache fixes (caches are 0.2.0 refactor state, fixed Mar 2026) — LEAD-RATIFIED after independent tag spot-check (warning string + caches confirmed ABSENT; min-shift read directly). Kept with correct framing: SBM Poisson mismatch (reframed), Alpine/musl include, both NUTS fixes (defects verified present at tag). RB-NA clarification mirrored in extract_inclusion_bf Rd. Parse gate re-run: shape identical to report 14. Spin-off: F-109 (the tag min-shift sparse-coding hazard — a TRUE fix candidate). | MM read + lead tag-verification 2026-08-01; report 17; lead merge check 2026-08-02 |
| F-097 | minor | fixed-verified (merged, brief 17; SEVEN sites made self-contained — the agent found 4 more port-referents beyond the named 3) | Four zratio source comments delegate their ground truth to "the companion" — the private development codebase a repo reader cannot open: `zratio_law.h:24` ("tableless C++ port of the companion's build-time CN anchor engine"), `:47` ("matching the companion's c(diff(x), 0)/2 ..."), `:61` ("the engine's eta is the companion's beta directly"), `zratio_engine.h:229` ("matching the companion's gold"). Public USER docs are clean — lead-grepped man/, vignettes/, R/ roxygen: zero hits. Surfaced by MM's brief-08 read (the wall he hit). Fix: rewrite the four comments self-contained — state the formula, the convention, and what "gold" is, with no external referent; no code changes. SCOPE MOVED by F-099: the three `zratio_law.h` sites get their rewrite ON THE ARCHIVE BRANCH (the file leaves the package); only `zratio_engine.h:229` is fixed in-package. MAINTAINERS' architecture map notes the law as archived (branch pointer). | MM read + lead verification 2026-08-02; F-099 |
| F-098 | major (maintainability / bus factor) | open (fix: brief 18, then 08 v2) | The hierarchical-prior zratio machinery's DERIVATIONS are not assembled anywhere a reader can follow. The math was developed in agent conversations against the private companion; the in-repo record is scattered across `dev/plans` (`2026-07-02_hier-zratio-approximation_PLAN.md`, `2026-07-13_zratio-calibration-redesign_DESIGN.md`, `2026-07-22_zratio-surface-b-migration_PLAN.md`, `2026-07-14_zratio-calibration-and-trust-gauge_NOTE.md`, `2026-07-06_*` analyses) and `dev/audit` PR reviews — no single self-contained derivation of the DEPLOYED chain exists. Surfaced by MM's brief-08 read: the maintainer can follow derivations but has none to follow. COMPOUNDED by a lead error, recorded: brief 08 v1's reading order led with `zratio_law.h` — whose own header declares it DORMANT (not the deployed anchor source, "not used in the paper") — so the maintainer started in the hardest file in the subsystem, off the deployed path. Fix: brief 18 assembles the curriculum under derive-source-or-FLAG rules (invented math is the failure mode), the lead verifies it against code + the report-04 certificates, and brief 08 v2 runs on top, interactively. | MM 2026-08-02; lead |
| F-099 | decision | fixed-verified (merged, brief 17; archive branch `archive/zratio-analytic-law` PUSHED at integration) | REMOVE the dormant analytic law from the package; ARCHIVE it on its own documented branch. MM: "Now it's extra baggage, it even has a test (which several agents are trying to trim), and it is a burden on maintenance for software that is not used." Lead-verified seams: exactly ONE include in the tree (`src/zratio_test_interface.cpp:14` — the test interface itself); nothing on the deployed path touches it. Removal set (5): `src/models/ggm/zratio_law.h`; the `zratio_law_moments` export block in `zratio_test_interface.cpp` (+ regenerated RcppExports cpp/R); `tests/testthat/test-zratio-law.R`; `tests/testthat/fixtures/zratio_law_reference.rds` + `make_zratio_law_reference.R` (the generator needs the private companion to run — dead weight in-package). Archive branch `archive/zratio-analytic-law`: pre-removal state + the F-097 self-contained comment rewrites + an archival note (why, re-wiring condition = the large-q crossover the header describes, fixture regeneration needs `sv/ggm_paper/R`); lead pushes it at integration. NO NEWS entry — never deployed, never user-visible (the never-shipped rule, applied to code). Supersedes brief 12's tier classification of `test-zratio-law.R`: that block vanishing from both tiers is AUTHORIZED here, not a silent drop. Brief 18's curriculum appendix documents what it was and where it lives. | MM 2026-08-02; lead seam-verification |
| F-100 | major | fixed on develop (merged `a1c0e768`, brief 20 — harness 32/32 green) | The weekly-compliance harness went RED on its FIRST run against rc1-main (2026-08-02 schedule, run 30735056271): `Error: Can't find property <bgms>@posterior_coclustering_matrix` on fixture 1 of 32. DIAGNOSED: a property-name skew — the harness (`tests/compliance/test_compliance.R:428,495`, `generate_fixtures.R:507`) accesses `posterior_coclustering_matrix`, but the current S7 class names it `posterior_mean_coclustering_matrix` (see `test-bgmCompare.R:302`); S7's `$` errors on unknown properties instead of returning NULL. The harness matched pre-freeze main (0.1.6.3-era) and was never swept when the class renamed. Fix on develop: sweep EVERY property name the harness touches against the current class (`posterior_mean_allocations` at `:495` is suspect too), regenerate/re-baseline fixtures as needed. NOTE the schedule mechanics: the workflow fires from MAIN's file with main's checkout, so Sundays stay red until the release re-merge — pre-announced noise, same as the old nightly. Also worth one line in the F-002 memo: any downstream code using the OLD property name errors on 0.2.0.0 fits. RESOLVED (brief 20, `4386f545`): THREE skews, not one — (1) `posterior_coclustering_matrix` was NEVER correct (the tag also stored `posterior_mean_…`, tag `output_utils.R:142`; only its roxygen said otherwise), so every committed SBM fixture carried a silently-NULL coclustering matrix; (2) compare-side `posterior_mean_indicator` exists on NEITHER side — removed, coverage kept via `posterior_summary_indicator`; (3) `posterior_mean_{main,pairwise}_differences` are new since 0.1.6.3 — explicit `new_since_baseline` skip until a baseline carries them. Plus: dead `migrate_fixture()` removed (its rename table mapped every name to itself); `generate_fixtures.R` now PINS the 0.1.6.3 baseline (CRAN Archive fallback + hard stop — lead-ratified; prevents vacuous self-baselining once 0.2.0.0 ships); coclustering/allocations now actually compared in `check_structure()`. FORWARD-COMPAT ANSWER FOR 19's MERGE: the harness never reads `fit$arguments` — 19's recode map + support table are SAFE, nothing to coordinate; the only strict comparison is the currently-dead `identical()` at `test_compliance.R:606` (see F-116). | CI run 30735056271; lead diagnosis 2026-08-02 |
| F-101 | major | fixed-verified (merged, brief 12) | The develop push-check coin-flip, ROOT-CAUSED at 12's merge — and the lead's original attribution was WRONG: all three red `R-CMD-check` runs carry exactly ONE failure, `Failure ('test-mcmc-diagnostics.R:434:3')` — the RB-Rhat-is-classic-split-Rhat identity — not `test-zratio-surface-build.R:149` (that was a SKIP line the lead's log grep matched; develop's `:149` is the fence block's slow-gate `skip_if`, which cannot fail; correction verified against runs 30719444201 and 30720323522, Linux jobs only). Mechanism: the identity was pinned at `1e-8`; both sides sum the same autocovariances in different orders, so the last digits follow the runner's BLAS — Linux lands at ~3.1e-8 while macOS holds the pin, and heterogeneous ubuntu-latest hardware turned that into a per-run coin flip on identical content. Brief 12 hit the same failure independently behind the old timeout (its run 30719120127) and FIXED it (`2af3c68c`): tolerance widened to `1e-6`, in-file reasoning notes the identity guards df-adjustment/wrong-split defects that move Rhat by ~1e-2 — four orders above the new pin. Lead verified the fix diff and both red-run signatures; push-checks should stop flaking from `f24ad3c8`. The zratio-surface-build socket-cluster intermittent this row previously conflated (observed only on brief 12's own branch runs, never confirmed in a develop push-check) is now its own finding, F-104. | CI runs 30719444201/30719697671/30720323522; report 12 finding 3; fix `2af3c68c`; lead 2026-08-02 |
| F-102 | minor (process) | fixed-verified (merged, brief 17; all FOUR push-triggered workflows) | Every push to develop triggers the full r-lib trio — lint (~10 min), R-CMD-check (~50 min), test-coverage (~60 min) — including pushes that touch only `dev/` review records: ~12 record-only pushes on 2026-08-01/02 burned ~24 CPU-hours re-checking identical package content, and F-101's coin-flip turned that into the wall of red the maintainer saw. Fix: `paths-ignore: ['dev/**']` on the push triggers of those three workflows (they are separate files from brief 12's nightly/certification/fast-checks set — no contention). SCOPE +1 (lead, at 12's merge): the merged `fast-checks.yaml` also triggers on every develop push with no paths filter — the same `paths-ignore: ['dev/**']` applies to it as a FOURTH file (dev/ files cannot change T0 outcomes); its `pull_request` trigger stays untouched. | lead 2026-08-02 |
| F-103 | major | ESCALATED TO MM (brief 13 stopped per instruction — product constant, not a test constant) | `zratio-gauge` harm-threshold detector FAILS on Linux CI, passes on macOS/local: `test-zratio-gauge.R:243` "the deployed surface holds that fixture under the harm threshold" — `pc$harm_pred` 0.01071 >= threshold 0.01000 (7% overshoot; `harm_flag` flips TRUE). Discovered by brief 12's first full T1 run (30715557912) — the block sat behind the old nightly's 120-min timeout and had NEVER executed on CI. Parked in T2 for greenness AGAINST the tier contract (the gauge detector is a named T1 concern at ~1.5 min — not a cost demotion; report 12 finding 2, explicitly flagged as such). Same species as F-080/F-081: platform/scheduler sensitivity meeting a tight statistical threshold. Brief 13: diagnose whether the fixture's `harm_pred` drifts by platform (RNG path/BLAS in the assessment sweeps) or the threshold/fixture is mis-founded; fix and RESTORE TO T1. If it turns into a harm-threshold policy question (product constant, not test constant), escalate to MM. ESCALATION (2026-08-03, report 13): NOT platform drift — the negative-control fixture's harm_pred spans 0.0004–0.0125 across 12 seeds at the shipped size, the 0.01 threshold sits INSIDE that spread (Linux's 0.01071 is an unremarkable draw), the alarm fires on 9/36 healthy fits (~25%), and raising n_samples does not shrink it: the shipped alarm's false-alarm rate is a PRODUCT CONSTANT. Block stays T2 (a 1-in-4 flaky assertion cannot gate pushes); the audit-sweep lever is named in the report. COUPLED TO F-010: the hierarchical flip makes the gauge the default experience — brief 24's drafting is GATED on MM's alarm ruling. | report 12 finding 2; run 30715557912; report 13 |
| F-104 | major | fixed (merged `8bfa6148`, `030d830a` — the surface build settles its worker count ONCE and reads the cluster size back, hardening the cores-did-not-reach-the-constructor path; original error never reproduced, hardening is preventive and says so; brief-12 probe kept; T0 proof accrues on CI) | `test-zratio-surface-build.R` socket-cluster block ("the socket-cluster build path matches the serial build") — UNEXPLAINED INTERMITTENT, T0-resident (ungated), so it gates every push/PR via fast-checks and R-CMD-check. Observed ONCE: brief 12's run 30719120127 errored `4 nodes produced errors; first error: could not find function "zratio_anchor_cn"`; runs 3/4 passed on identical content. The agent's first diagnosis (PSOCK workers need an installed bgms → `local::.` in workflows) was RETRACTED — run 4 passed without it on a deps-only runner; two follow-up hypotheses killed (no transitive CRAN bgms reachable from Suggests + no version string in any log; not a silent skip — the constant 3 skips are the three T2 blocks). FIRST-CLASS CLUE: the erroring run reported **4** PSOCK worker nodes for a `cores = 2L` call — the cores argument did not reach the cluster constructor on that run. Interim shipped shape (brief 12, merged `f24ad3c8`): a 1-node PSOCK probe asks `requireNamespace("bgms")` on a worker and skips visibly if unloadable — monotone safe (can only turn an error into a visible skip, never a failure into a pass; lead verified in source), NOT a fix, and it makes the block's T0 coverage conditional — watch for skip-count wobble on fast-checks (a permanently-skipping block would masquerade as health). `local::.` was dropped because it cost ~10 min of every nightly. Brief 13 owns, with F-080/F-081/F-103. | report 12 finding 3; brief-12 status relay; lead 2026-08-02 |
| F-105 | minor | closed (workarounds removed `b0c4b431`, merged `8bfa6148`; pixel-identical, no snapshot churn) | `format_log_bf()` renders a Bayes factor that rounds to zero from below as "-0.0". The function is defined in `R/verdicts.R` (off-limits to brief 11's batch) and `print()` reaches it directly, so the guard belongs IN the function; brief 11 shipped call-site workarounds in the plot files as a stopgap (documented at `plot_style.R:411`) — brief 13 removes them once the function guard lands. | report 11 |
| F-106 | minor | closed (rechecked at 11 integration 2026-08-03: ring clause STANDS — MM's round-2 reversal restored rings; the adjacent stale edge-posterior sentences — `data|H1`/`data|H0` labels, caption — patched in `66f7d17f`) | NEWS.md describes the compare network display as encoding groups/differences via square-vs-circle NODES — the code has never drawn that (stale before brief 11's single-renderer unification, doubly so after). Same family as F-082..F-085 (false NEWS claims). Replacement clause text proposed verbatim in report 11. | report 11 |
| F-107 | minor | closed (argument removed outright `11eb07e1`, merged `8bfa6148`; never shipped, no deprecation cycle, no NEWS) | `plot_edge_posterior(evidence_threshold=)` is INERT after the ratified verdict-word removal — the argument only drove the dropped annotation; brief 11 kept it documented-as-inert. Lead decision: `plot_edge_posterior()` is NEW in 0.2.0.0 (never shipped at 0.1.6.3), so the never-shipped rule applies to API too — REMOVE the argument outright, no deprecation cycle; Rd and call sites with it. Brief 13. | report 11; lead 2026-08-02 |
| F-108 | minor | fixed on develop (merged `f3f18941` via brief 11 round 2: the `plot.bgms` nothing-to-draw stop() is removed, an all-absence fit draws with the absence panel filled, the two methods agree; MM ratified by ruling and by round-3 render verdict) | Behaviour divergence on the nothing-to-draw case: `plot.bgms` ERRORS when no edge reaches presence or undecided, while `plot.bgmCompare` draws the empty frame with an honest subtitle ("no difference reaches presence or undecided"). Lead recommendation: adopt compare's convention in `plot.bgms` — an all-absent/undecided network is a RESULT, not a failure; the render pair `compare-difference-empty-*` shows the convention in action. MM judges alongside the renders; implementation brief 13. CLARIFICATION EXCHANGE (2026-08-02): MM first read this as the SELECTION-OFF case and ruled that adjacent case instead — `edge_selection = FALSE` fits plot the FULL network with edge WEIGHTS ("It is a non-inclusion-BF analysis then") — adopted as a NEW revision-round rule (addendum A to brief 11: evidence encoding only for selection-on fits; object label carries the distinction, "Edge evidence" vs "Edge weights"). The actual F-108 question RE-EXPLAINED; then DECIDED (MM, 2026-08-02): "Draw empty network. That is the honest plot." — and under the docs-site's three-panel edge evidence display the case DISSOLVES BY CONSTRUCTION: an all-ruled-out analysis fills the ABSENCE panel (MM: "an empty plot will just show only edges with evidence for absence"). `plot.bgms`'s error is removed by the brief-11 redesign itself, not as a separate brief-13 item; the hold instruction (addendum B) is lifted by the corrective addendum. | report 11; MM decisions 2026-08-02 |
| F-109 | minor | fixed-verified (merged `a1c0e768`, brief 20; NEWS `121890e5`) | TRUE bug-fix candidate surfaced by 17's tag verification: `cran-0.1.6.3`'s `recode_data_for_prediction()` recodes ordinal `newdata` by MIN-SHIFT (`x - min(x)` when min > 0, `R/simulate_predict.R:1238-1252` at the tag — lead read the code directly), so a SPARSE original coding (gaps, e.g. 1,2,4,5) miscodes: the fit's recode collapses to 0..3 while min-shift gives 0,1,3,4 — `predict()` on original-scale sparse data lands on the wrong internal categories, silently. 0.2.0's stored-map recode appears to fix this. Brief 13: VERIFY current behaviour on a sparse coding (round trip + original-scale predict), and if confirmed, add the TRUE NEWS Bug-fixes entry (0.1.6.3 framing) — the honest replacement for the false category-scale entry 17 dropped. RESOLVED (brief 20, `aa9cb9cb`): PROVEN BOTH ENDS — current develop recodes through the stored map (`R/simulate_predict.R:1046-1092`, `arguments$category_levels`; inverse `:1110-1134`); tag-side runtime demo on codes 1,2,4,5: value 4 → category 3 and value 5 → category 4 (nonexistent in a 4-category model), silent, predictive rows shifted up to 0.046 — and tag `simulate()` returns INTERNAL codes, so its own round trip leaves the user's scale; regression test = section 8 of `test-simulate-predict-regression.R`; NEWS Bug-fixes entry landed. | report 17 §2 finding 1 caveat; lead 2026-08-02 |
| F-110 | blocker (with F-075) | fixed on develop (merged `02c609c3`, brief 19 — classed `bgms_group_support_warning` + true-gap message from `build_spec_compare()` + `category_support` stored on the fit; warning text lead-verified: names each variable/category/group, says the difference is prior-set, scopes it to thresholds only; the classed-condition API addition RATIFIED — better than muffling 14 tests; residue: warning fires routinely on 20–25-row groups (correct but frequent — Rd mention of the class = MM-optional); bgm()'s own silent gap renumbering = F-118) | The category collapse is ENTIRELY SILENT: no `warning()`, `message()`, or condition anywhere on the path; nothing in `verdicts()`, `summary()`, or the comparison vignette; the only trace is comparing `extract_arguments(fit)$num_categories` against one's own data. Fix (report 16 §5.2, required INDEPENDENTLY of the recode-semantics decision): warn whenever any variable's category count is reduced — naming the variable, the map, and merged observation counts — and store the map in the fitted object so summaries can surface it. | report 16 finding 16-2 |
| F-111 | minor | fixed on develop (merged `02c609c3` — Rd union semantics + structural-zero consequence + BC rationale, vignette note; `4fa472bd`) | `man/bgmCompare.Rd:62` says "For ordinal variables, unused categories are collapsed" — false: categories UNOBSERVED IN ANY ONE GROUP are merged, however well observed elsewhere (lead re-read the Rd line verbatim). After the F-075 fix the sentence must describe the NEW semantics; the comparison vignette gains the matching note. | report 16 finding 16-2 |
| F-112 | major (test infrastructure) | fixed on develop (merged `02c609c3` — `bgmCompare_test_logp_and_gradient()` hook + finite-difference gradient test incl. a group-differing-support case; `adc7c1fd`) | `src/models/bgmCompare/` exposes NO `[[Rcpp::export]]` test hooks while GGM and mixed ship `*_test_logp_and_gradient()` — the compare log-posterior/gradient cannot be unit-tested against an independent reference (this is WHY localizing F-075 needed indirect probes). And `tests/testthat/test-collapse-categories.R` passes nine mechanics cases while asserting nothing about estimate survival — a function that turns five levels into binary passes its own suite. Fix: add `bgmCompare_test_logp_and_gradient()`; extend the collapse test with an estimate-quality assertion vs matched bgm() fits. | report 16 finding 16-3 |
| F-113 | question (release-impact) | resolved (2026-08-03, brief 19 task 8 scan) | The rate that matters: how often do REAL group comparisons have a variable whose observed support differs by group? Wenchuan splits: never. Boredom: lead-checked 2026-08-02 — language groups share support on every variable, NO collapse (population splits share support). Clinical-vs-control data: plausibly often (location differences are the point of such comparisons). Check available multi-group datasets (the docs-site ADHD compare fixture is the natural candidate) before release; ALSO scoped here: the damage GROWS with the number of groups (intersection over all K — report 16 open q4), and Blume–Capel variables are EXEMPT from collapsing (`validate_data.R:393`) — whether that asymmetry is intended is MM's question (q5). RESOLVED: no shipped dataset's NATURAL grouping trips the defect (ADHD is entirely binary — the docs-site comparison was never affected; Boredom language groups share support; Wenchuan splits never) while a MEDIAN-SPLIT of Boredom trips 2/8 variables — the defect bites exactly when groups differ in level, as the fix story predicts; q4 moot under union semantics (no compounding over K); q5 settled by MM decision 2 (BC exempt deliberately, scores meaningful). | report 16 open questions 3–5; lead Boredom check; report 19 §5 |
| F-114 | minor | fixed on develop (merged `a1c0e768`, brief 20; NEWS `121890e5`) | `extract_arguments()` returned no `main_effect_indices` for bgmCompare objects — surfaced when 16's `33_mains.R` errored on it (report 16 §7.iv). RESOLVED (`83782eaa`): PREMISE CORRECTED — `bgm()` fits never populate the field and need no mirror (their main effects are already one row per variable); the compare layout lives in `cache$main_effect_indices` (`build_spec.R:580-592`, `build_output_compare.R:46`) and is now surfaced by `extract_arguments.bgmCompare()` with graceful fall-through for pre-field fits; Rd documented + tested. LEAD RULING (2026-08-03, report 20 §6.3): accessor-side placement STANDS — no `$arguments` write at 19's merge (derived value; keeps clear of the builder 19 extends). | report 16 §7; report 20 |
| F-115 | minor | closed (merged `8bfa6148` — device-size sentence in both Rds + bgms.verbose narrow-device message, `82a20f56`; no geometry change, per decision) | The three-panel evidence display SQUEEZES at R's default ~7×7 device: each panel gets ~1:3 aspect and one panel's outer nodes cross into its neighbour — pre-existing geometry (not the legend overlap MM flagged, which is fixed), rooted in qgraph writing its coordinate range from `mar` while drawing nodes at PHYSICAL size; the brief-11 agent built and tested an aspect-matched coordinate box that did NOT resolve it, and declined magic-constant guesses at qgraph internals (the F-070 trap — correct restraint). All review renders at 13.5×5.2 where it does not arise. Candidate remedies for 13: recommended-device-size note in both Rds (minimum), and/or a one-line message on narrow devices, and/or vertical stacking below an aspect threshold (MM's look decides how far to go). NOT a regression — the tag ships no plot methods at all (lead-verified: 0.1.6.3 NAMESPACE exports none). | brief-11 round-3 finding M; lead tag check 2026-08-02 |
| F-116 | minor | decided (documented; no release action) | Compliance fixtures are NOT machine-reproducible (brief 20, measured): same seeds + same 0.1.6.3 source differ from the committed baselines from iteration 1 (max diff 0.66 on `posterior_mean_main`); NOT `cores` (1/2/detectCores bit-identical, measured), NOT the data (`identical()` across versions); toolchain FP cascading through the fixed-step leapfrog — a source build and the CRAN binary agree on-machine and both differ from the old committed set. The committed baseline is now the brief-20 machine's 0.1.6.3 behaviour; harmless under structure-only comparison (32/32 green, machine-independent). LEAD RULINGS (2026-08-03, report 20 §6): accept for release; runner-side regeneration (`ubuntu-latest`, needs a regenerate job on `weekly-compliance.yaml`) = optional Phase-3/post-release step and a PREREQUISITE for restoring bitwise comparison (harness notes 8/12); the strict `identical()` at `test_compliance.R:606` (compare `raw_samples`, currently dead code) surfaces only at bitwise restoration; a current-version second baseline to cover the `_differences` means (§6.4) is a different harness, out of release scope. | report 20 finding 5, §6 |
| F-117 | minor | report landed (2026-08-03, branch `fix/summary-zero-marking`, 2 commits, gate 0F/0W, no snapshot churn) — MERGE GATED on three MM decisions: (a) NARROW rule as shipped vs WIDENED (empty reference cell cascades to all of that variable's rows — the agent's own fixture shows the narrow rule missing the worst rows: 2.431/1.738 unmarked, order-of-magnitude inflated; four-line change ready on the branch; lead recommends WIDEN, and the fit-time warning text then widens too — `validate_data.R` is free now); (b) footnote wording (current "this group" fails on contrast-major rows; lead proposal: "a group lacks observations in this category or in the reference category; the estimate reflects the prior, not the data"); (c) the printed format itself (leading *, one footnote) | Structural-zero presentation: a group-by-category threshold difference with NO supporting observations settles where the Cauchy difference prior stops it — measured posterior means 5.9 (sd 2.2) / 14.3 (sd 4.7) on a deliberately adverse 3-variable fit (report 19 §19-3). The classed warning explains it and `category_support` locates it, but `summary()`'s main_diff block prints those rows UNMARKED. Options (each changes reported output; none authorized in 19): mark the rows, suppress them, hard-zero, or category-specific shrinkage. Lead default was ship-as-is; MM RULED (2026-08-03): mark now — asterisk + one footnote line reading "no observations in this group for this category; the estimate reflects the prior, not the data"; the deeper resolutions stay recorded as post-release research directions: report-the-known-quantity (fitted P(cat|group)≈0 instead of a difference), hierarchical partial pooling on differences, and common-support difference semantics; the NUMBER-SIZE cause is separately addressed by F-119's Normal-prior switch (Cauchy tails are what let these cells reach 5.9–14.3). | report 19 §19-3, OQ1; MM 2026-08-03 |
| F-118 | note | open (post-release candidate; disposition sweep) | `bgm()` silently renumbers unused interior ordinal categories (`reformat_ordinal_data()` closes gaps with no message) — the behaviour is documented for bgm ("unused categories are collapsed") so this is a MESSAGE-PARITY nicety with the compare path's new true-gap message, not a correctness defect. Shared-path change; deliberately left out of brief 19. | report 19 §19-4, OQ3 |
| F-119 | decision (MM, 2026-08-03) | decided — brief 22, QUEUED behind 13's merge | bgmCompare's slab default moves Cauchy → Normal, mirroring bgm: develop has `bgmCompare(interaction_prior = cauchy_prior(scale = 1))` while `bgm(interaction_prior = normal_prior(scale = 1))` (lead source-checked; 0.1.6.3's compare priors were Cauchy throughout — `difference_scale`/`pairwise_scale` "Scale of the Cauchy prior" at the tag, so the change is tag-visible and gets the MM-directed one-line NEWS entry). MM's rationale: the Cauchy's heavy tails are exactly what lets data-free structural-zero differences run to 5.9–14.3; a Normal partially fixes them to zero (ridge-style shrinkage) while data-rich parameters stay likelihood-dominated. Brief-22 scope: flip the default; map EVERY parameter family the slab governs (baseline pairwise, pairwise differences, main-effect differences — confirm which draw from `interaction_prior`); sweep Rd/tests/extract_indicator_priors; Cauchy stays available as an option; 3-seed spot re-measure from `val16` truths to confirm OC parity transfers to the new default (full 10 seeds if anything drifts); adverse-fit before/after showing the structural-zero rows shrink. QUEUED behind 13 because a default-prior change churns the compare test surface 13 is editing (tolerances, planted-δ). MOTIVATION (settled after the same-day F-120 detour and reversal): the structural-zero rationale is BACK as primary — the Normal is the regularizer for boundary-sitting zero-support cells (moderate values instead of Cauchy-tail 5.9–14.3) — alongside bgm-consistency; brief-22 scope = the switch + two MM-requested artifacts (2026-08-03): (i) RIDGE VISUALIZATION — joint (mean, difference) contours for the adverse structural-zero cell, grid-evaluated EXACTLY via the F-112 logp hook (other parameters held at posterior means): the likelihood ridge alone, the posterior under Cauchy, the posterior under Normal — the identification issue and its regularization made visible; report asset, candidate explanation material later (MM-owned); (ii) DIFFERENCE-TEST EVALUATION on zero-support cells: direction and strength of the inclusion BF vs the non-observing group's size and the category's rate under a TRUE shift (the one-sided-evidence case), the SAMPLING-zero null case (shared distribution, rare category, small groups — does the verdict over-call a difference that isn't there?), and slab sensitivity (Cauchy vs Normal vs scale — the Bartlett zone), leveraging prior_sensitivity_check reweighting wherever it covers difference verdicts and refitting only where it does not; sequential, one fit at a time, grid thinned to budget with every drop reported. | MM 2026-08-03; lead source check |
| F-120 | design ruling (MM, 2026-08-03) | REVERSED by MM the same day — no δ-fixing; final resolution = F-117 mark + F-119 Normal default | STRUCTURAL-ZERO IDENTIFICATION, MM's correction of the lead's framing: the compare model estimates an overall mean and a group deviation per category-threshold; with the category observed in only ONE group those are two parameters for ONE identified quantity — "there are no two random objects here, only one" — so the model is FIXED, not marked around: the group deviation for a category unobserved in a group is FIXED AT ZERO (identification constraint, unbiased; supersedes the prior-driven behaviour merged in 19 AND the lead's ship-as-is/mark framing of F-117). Selection is unaffected in principle: δ ≡ 0 under H0 and H1, so the cell's indicator carries no evidence and is EXCLUDED from difference selection; `verdicts()`/summaries report "fixed" for it, never a BF. K>2 "slightly more involved, only mildly": deviations defined over the OBSERVING subset (re-centered constraint), zero for non-observers — exact scheme proposed by the brief-22 agent from the existing constraint code, MM RATIFIES before merge. Consequences folded into brief 22: sampler/prep skip fixed cells (src/models/bgmCompare + R prep; `category_support` already locates them); the 19 warning text rewritten (no longer "prior decides" — now an information note that the difference is fixed at zero; keep the classed condition); summary shows the fixed zero with the F-117 mark + footnote "difference fixed at zero — category unobserved in this group" (F-117's marking machinery carries over, its prior-driven footnote text does NOT); F-112's gradient hook validates the constrained model on a zero-cell case; adverse-fit before/after in the report. The 10-seed OC gate is untouched (its fits have full support). REVERSAL (MM, 2026-08-03, verbatim): "Zero is a valid observation under the model, just like n is, but in both cases the implied mle sits at the boundary." A zero count is DATA, not structure — it carries directional information about the deviation — and a boundary-sitting posterior is exactly what the prior regularizes; fixing would misread a valid extreme observation as an impossibility. FINAL: no sampler change; F-117's mark proceeds as issued (brief 21) and F-119's Normal default is the regularizer (brief 22, after 13). | MM 2026-08-03, ruling and same-day reversal both verbatim in row |
| F-121 | **blocker** (MM, 2026-08-03: "so the same seed leads to different results? That is a blocker.") | open — diagnosis relayed to the brief-21 agent (it holds BOTH disagreeing builds live); UB → immediate fix batch; clean FP → mechanism goes back to MM, blocker status is HIS to lift | bgmCompare draws are NOT reproducible across BUILDS (report 21 §3.4, isolated in three runs): same machine, same compiler, byte-identical `src/Makevars`, same seed, `chains = cores = 1` — swapping ONLY `bgms.so` between two compilations of identical source flips posterior means (2.431 vs 2.865); each binary is internally deterministic (base R code on the agent's build reproduces the agent's numbers exactly). Sharper than F-116 (machine-to-machine): this is build-to-build on ONE machine. Localization hint: report 20 §3.3 found the bgm-side tag source-build ≡ CRAN binary bit-for-bit on the same machine — so the effect is compare-path-specific or configuration-specific. What it does NOT break: the user-facing promise (same installed binary + same seed = same numbers) holds. What must be EXCLUDED before accepting: undefined behaviour (uninitialized read, order-dependent accumulation) — legal compiler FP variation (FMA/scheduling) is acceptable under F-116's doctrine, UB is a bug and CRAN's own clang-UBSAN would find it post-submission. DIAGNOSIS (reassigned from 22 to the 21 relay — that agent holds both disagreeing builds in wt-fix12/wt-fix12-base): (1) UBSAN+ASAN build, compare tests + the exact seeded fit; (2) OBJECT-FILE BISECTION — swap .o files between the two existing builds until the flipping translation unit is found, then diff its disassembly; (3) mechanism verdict: UB (bug — immediate fix batch, blocker holds) vs legal FP variation (compiler scheduling/FMA — mechanism named to MM, who decides acceptance; the F-116 doctrine does NOT auto-lift a blocker MM declared). Snapshots already carry no MCMC numbers (agent's defensive choice, correct). | report 21 §3.4 |
| F-122 | informational (positive) | recorded | Task-8 surprise (brief 13): the planned tag-vs-rc1 ordinal estimate-agreement table could not be produced because 0.1.6.3 IS THE ONE FAILING — max R̂ 12.3 / Inf / Inf and min ESS 1.7 / 0 / 0 on three of five seeded synthetic datasets, while rc1 is clean on all five AND closer to the generating truth on all five. Positive release evidence (the sampler rewrite fixes real convergence failures); candidate line for cran-comments; caveats in report 13. | report 13 task 8 |
| F-123 | moderate | decided (MM 2026-08-03: "(a) no; (b)") — reject `p == 0 \|\| q == 0` at the top of `build_spec_mixed_mrf()` with a clear message + totalize the unguarded loop bounds (model :851/:858, gradient :31/:87/:146/:498/:570, all lead-verified); (a) support-the-degenerate-block REJECTED outright, not deferred; rides brief 24 as a micro-task | Mixed MRF machinery is UNDEFINED at a degenerate block (report 23 F1; NOT user-reachable — `bgm()` hardcodes `model_type = "omrf"` (R/bgm.R:664) and `bgm_spec` re-resolves `is_mixed = has_continuous && has_discrete` (R/validate_model.R:74-76), so p ≥ 1 and q ≥ 1 for every reachable mixed fit; verified by lead against source). All-ordinal (q=0) + edge selection: `for(size_t i = 0; i < q_ - 1; ++i)` at src/models/mixed/mixed_mrf_model.cpp:858 wraps to SIZE_MAX on unsigned `q_` → ~1.8e19-iteration NO-OP SPIN, never returns (agent stack-sampled 2387/2387 in `get_vectorized_indicator_parameters()`; ~600 years at 1e9 it/s; no OOB write — inner loop empty); selection off errors on a zero-width Cholesky subvec (:630/:775); all-continuous (p=0) errors in R before C++ (build_spec.R:310-314 hands zero-column discrete part to `reformat_ordinal_data`). Three of the four Gyy-triangle traversals in the SAME file are guarded (`i + 1 < q_` in the RB mirrors :884/:898 whose comment says they mirror the broken one "exactly"; `if(q_ >= 2)` at :1129/:1201) — oversight, not decision; same unguarded shape in mixed_mrf_gradient.cpp (:570 `q_-1`; :31/:87/:146/:498 `p_-1`). Cost to review: blocked brief 23's sharp all-ordinal reduction; substitute + measured confound probe certified instead. Fix shapes (report Q2): (a) support the degenerate block — totalize loop bounds AND fix the chol subvec AND the R-side split AND `delta = 0.5·log(num_continuous)` = log(0) — a mini-project whose payoff is making the sharp reduction runnable; (b) reject `p == 0 || q == 0` at the top of `build_spec_mixed_mrf()` with a clear message + totalize the unguarded loop bounds as hygiene — two small changes. Report's F2 (cores default) = known territory: F-018 covers the check path, the default itself is brief 08's defaults review; no new row. Report Q3 (cross-block planted-zero PIP max 0.455 vs ≤0.093 pure blocks, one draw of 30) recorded as informational, no row. | report 23 §5.1, Q2, Q3 |
| F-124 | informational | open — characterization needs an ordinal-only n×p sweep, outside 23's remit; post-release candidate or slot-filler task if a future batch has idle sequential time | Ord-ord recovery slope 1.164 at n=1000, p=3 ordinal in the mixed task-4 design (report 23; per-seed 1.055/1.165/1.145/1.103/1.351, sd 0.111 ⇒ ≈3 se above 1; both planted ordinal edges inflated in the same direction, 0.400→0.469 and −0.300→−0.345) while the continuous block in the SAME fits is unbiased (0.993) and cross sits between (1.039). NOT a mixed-path artefact — task 2 pins the mixed discrete block to the OMRF discrete block at 0.4%, so both paths carry it equally: property of the ordinal MRF estimator itself. NOT visible on the p=6 n=1000 all-ordinal task-2 design (recovery slope 0.9876, per-edge scatter 0.76–1.18). Runs AGAINST the slab's shrinkage, so not prior-induced. Unseparated candidates: pseudolikelihood finite-sample bias at small p; interaction with the cross-block coupling (each ordinal variable tied to a continuous one); slope carried by two truth values. Detection unharmed (10/10 planted found, 0/5 false presences). | report 23 §4.3, Q1 |
| F-078 | major | fixed-verified (merged `6ed36b44`) | `plot_edge_posterior()` could not name a mixed fit's cross edge in variable order: the label was built `variables[first]-variables[second]` in COLUMN order with membership tested in one orientation only, but a mixed fit lays pairwise draws out by block and names a cross edge by its DISCRETE end — the pair `(c1, d2)` is stored `"d2-c1"`, so a real, decisive edge (log BF ≈ 30) errored "No edge between 'c1' and 'd2'". Pre-existing, NOT introduced by the redesign; same family as F-047 (both ends resolving to one variable) and F-037 (reading a mixed fit's indicators in its own layout). Fix: `edge_column_label()` tries both orientations, in the pairwise draws and in the `verdicts()` table; regression test on the mixed fixture. (Report 10's local IDs collide with the master list — its "F-068" = this row.) | report 10 finding 2 |
| F-079 | major | fixed (merged `6ed36b44`; refusal stands) | `plot_edge_posterior()` silently drew a `bgmCompare()` fit as though its BASELINE pairwise effect were one network's edge: no class check, and `extract_pairwise_interactions()` returns baselines for compare fits — drawn with a zero spike (baselines are never excluded) under a title carrying a DIFFERENCE verdict from `verdicts()`. Picture and number were about different parameters. The redesign would have made it WORSE absent a guard: compare `extract_arguments()` has no `edge_selection` field, so the fit would have fallen into the Savage-Dickey branch — a licensed-estimator claim on a selection model. Now REFUSED with a message pointing at `plot(fit)` and `verdicts(fit)`. Beyond brief 10's written scope; the dots rule forced it; flagged, not assumed. The refusal exposes the real gap — a difference-edge panel sibling — filed as future_tasks item 42 (post-release). MM (2026-08-01), asked to ratify: "plot(bgmCompare_fit) needs to show Bayes factors as plot(bgm_fit) does" — lead reading: the refusal is acceptable, and the requirement is at the NETWORK level: `plot.bgmCompare` must display difference-BF evidence at parity with `plot.bgms` → brief 11 requirement (d); the single-difference-edge panel stays item 42 unless MM pulls it forward. | report 10 finding 3; MM 2026-08-01 |

## Details and verification notes

**F-001** — one-line fix in NEWS.md; verify by rereading `man/prior_sensitivity_check.Rd`
and `R/anchor_curve.R::assemble_curve()` after the edit. Fold into the NEWS
reconstruction task (PI) rather than fixing twice.

**F-002** — not a code change. Producing the memo is a review deliverable; the
five bgm() deltas were code-confirmed by BGMS-1 (see its defaults table) and are
re-verified in report 00c. Send after the defaults table is final.

**F-006** — the same wrong gloss existed in the docs site (routed to brief A6
there); the vignette is the in-package copy. Corrected reading is drafted in
`plans/a2-review-findings.md` and grounded in `src/mcmc_diagnostics.cpp:375-387`.

**F-012** — if the tarball diff shows real differences, every "unchanged since
CRAN" claim in the diff map must be rechecked against the true baseline.

**F-016 (major, decided, maintainability+process)** — `dev/audit/` (the July
2026 audit, all PR reviews, specs, decision records) is not tracked by git
(`.gitignore:25 dev/*`). **MM decided 2026-08-01: keep it private** — it stays
out of the public repo. Snapshot archived to
`../bgms-audit-archive/dev-audit-plans-2026-08-01.tar.gz` (3.4 MB; covers
`dev/audit`, `dev/plans`, and the loose handoff docs). Residual risk: both
copies live in the same Dropbox; optional hardening is a private git remote
(see MAINTAINERS backlog #1). Re-snapshot when the decision record grows.

**Tag-hygiene record (under F-014)** — the two pre-review tags were deleted
outright at MM's direction (2026-08-01), archival declined:
`ggm-release-eval-ref` → commit `252c52df` (tag object `e7a42ad8`; never-merged
GGM SBC/oracle gates, 2026-07-06); `scratch-buffer-logz-ref` → commit
`9c61b99c` (tag object `19d6394f`; parked OMRF/mixed scratch-buffer tip,
2026-04-29). The commits remain recoverable from the local/remote object
stores until garbage collection (`git tag <name> <sha>` restores). The two
`dev/plans/backlog` scratch-buffer documents cite the deleted ref; these SHAs
are now the only pointer.

**F-056 / F-057 (both from MM's first bgmCompare call, 2026-08-01)** — one
call, `bgmCompare(Boredom[,1], group_indicator = Boredom[,1], seed = 1)`,
exposed two independent input-handling defects, and both are instances of
patterns this review has already named:

1. *Convention divergence* (PLAN strategy #3). `bgm()` unpacks its edge prior
   inside `bgm_spec()`, downstream of `data_check()`; `bgmCompare()` hoists the
   equivalent two lines into its own body, upstream of every validator. The
   divergence is invisible until input is malformed, and then the two functions
   give different-quality errors for the same mistake.
2. *The workaround standing in for the test*. F-057's character/factor path has
   no coverage because all four fixtures hand-coerce with
   `as.integer(as.factor(...))` and a comment explaining the coercion — the
   authors met the defect, routed around it in test code, and never filed it.
   Grep-for-coercion-in-fixtures is a cheap audit worth running once across the
   suite; a fixture that pre-massages input is evidence about the API, not just
   about the fixture.

Both are small fixes held for the batch after brief 05 (nothing here touches
the nightly, and brief 05 is in flight). Verification for F-057 must include
the natural call on the shipped dataset, not only a synthetic factor.

*Measured 2026-08-01* against the rc1 install (`~/bgms-review/lib`; both
`R/bgmCompare.R` and `R/build_spec.R` are unchanged `v0.2.0.0-rc1..develop`, so
both defects are pre-existing, not merge fallout). Four calls, same data:

| call | result |
|---|---|
| `bgmCompare(Boredom[,1], group_indicator = Boredom[,1])` | `non-numeric matrix extent` (F-056) |
| `bgmCompare(x = Boredom[,-1], group_indicator = Boredom$language)` | `'bin' must be numeric or a factor` (F-057) |
| same, `group_indicator = factor(Boredom$language)` | `'bin' must be numeric or a factor` (F-057) |
| `bgm(Boredom[,1])` — identical bad input, other path | `x must be a matrix or data.frame.` ← the message bgmCompare owes the user |

Both documented workarounds run clean: `group_indicator =
as.integer(factor(Boredom$language))`, and the x/y split
(`x = Boredom[lang=="en", -1], y = Boredom[lang=="fr", -1]`).

**Nightly target correction (affects F-044/F-048/F-049 reading)** — report 05
caught an error in the lead's deadline model: `nightly-validation.yaml` uses
`actions/checkout@v5` with no `ref`, and a `schedule:` trigger checks out the
DEFAULT branch — which is `main` (remote HEAD verified). So the Monday
2026-08-03 03:00 UTC run executes rc1, not develop, and will show FOUR failures
(3× F-048 stale fences + F-049) no matter what lands on develop. The brief-05
merge still mattered — it is what makes a green develop demonstrable — but the
red Monday run is baked in. Mitigations: (1) the workflow has
`workflow_dispatch`, so a manual run on develop demonstrates the repaired tier
(expected: exactly one failure, F-049); (2) pre-announce Monday's red as
known-stale. Standing process gap filed under F-044: scheduled validation that
only ever runs the frozen branch cannot gate integration — point it at develop
(or run both) post-release. UPDATE 2026-08-01 evening: the dispatched develop
run never produced its verdict — cancelled at the 120-minute job timeout
(F-071). The "expected signatures" above are therefore unobservable on CI
until the timeout is raised; the authoritative tier state remains the local
gates in reports 05/06.
