% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/centrality.R
\name{extract_centrality}
\alias{extract_centrality}
\title{Extract Posterior Centrality}
\usage{
extract_centrality(bgms_object, measure = "strength", ...)
}
\arguments{
\item{bgms_object}{A fitted model object of class \code{bgms} (from \code{\link[=bgm]{bgm()}}).}

\item{measure}{Character; the centrality to evaluate. Currently \code{"strength"}
(default), the sum of the absolute weights of a node's edges.}

\item{...}{Passed to methods.}
}
\value{
A numeric matrix of class \code{bgms_centrality} with one row per
posterior draw and one column per variable, carrying the \code{measure} as an
attribute. Use \code{\link[=summary]{summary()}} for posterior means, credible intervals, and
the probability of being the most central node, and \code{\link[=plot]{plot()}} for the
ordered interval display.
}
\description{
Evaluates a node centrality on every posterior draw of the network, giving
the posterior distribution of each node's centrality.
}
\details{
The centrality is evaluated on the model-averaged pairwise draws, in which an
edge excluded at a given iteration contributes exactly zero. Structural
uncertainty therefore propagates into the centrality without any extra step:
a node whose edges are themselves uncertain gets a wide centrality
posterior, which is the honest summary. Conditioning on the included-only
draws instead would report each node's centrality in the models where its
edges happen to be present, which is a different and generally larger
quantity.

Strength centrality sums the absolute edge weights, so positive and negative
associations both add to a node's total involvement rather than cancelling.
}
\examples{
\donttest{
fit = bgm(Wenchuan[, 1:5], display_progress = "none")
strength = extract_centrality(fit)
summary(strength)
}

}
\seealso{
\code{\link[=extract_pairwise_interactions]{extract_pairwise_interactions()}} for the draws this is computed
from, \code{\link[=verdicts]{verdicts()}} for which of those edges the data settle

Other extractors:
\code{\link[=extract_arguments]{extract_arguments()}},
\code{\link[=extract_category_thresholds]{extract_category_thresholds()}},
\code{\link[=extract_ess]{extract_ess()}},
\code{\link[=extract_group_params]{extract_group_params()}},
\code{\link[=extract_inclusion_bf]{extract_inclusion_bf()}},
\code{\link[=extract_indicator_priors]{extract_indicator_priors()}},
\code{\link[=extract_indicators]{extract_indicators()}},
\code{\link[=extract_log_odds]{extract_log_odds()}},
\code{\link[=extract_main_effects]{extract_main_effects()}},
\code{\link[=extract_pairwise_interactions]{extract_pairwise_interactions()}},
\code{\link[=extract_partial_correlations]{extract_partial_correlations()}},
\code{\link[=extract_posterior_inclusion_probabilities]{extract_posterior_inclusion_probabilities()}},
\code{\link[=extract_precision]{extract_precision()}},
\code{\link[=extract_prior_inclusion_probabilities]{extract_prior_inclusion_probabilities()}},
\code{\link[=extract_rhat]{extract_rhat()}},
\code{\link[=extract_sbm]{extract_sbm()}},
\code{\link[=plot.bgms_centrality]{plot.bgms_centrality()}},
\code{\link[=summary.bgms_centrality]{summary.bgms_centrality()}}
}
\concept{extractors}
