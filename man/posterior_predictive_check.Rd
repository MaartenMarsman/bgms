% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predictive_checks.R
\name{posterior_predictive_check}
\alias{posterior_predictive_check}
\title{Posterior Predictive Check}
\usage{
posterior_predictive_check(
  bgms_object,
  statistic = c("margins", "pairwise", "sumscore"),
  ndraws = 500,
  probs = c(0.025, 0.975),
  seed = NULL,
  cores = parallel::detectCores(),
  display_progress = c("per-chain", "total", "none"),
  ...
)
}
\arguments{
\item{bgms_object}{A fitted model object of class \code{bgms} (from \code{\link[=bgm]{bgm()}}) with
discrete variables.}

\item{statistic}{Character vector; which statistics to check. Any of
\code{"margins"} (the category proportions of each variable), \code{"pairwise"} (the
Spearman correlation of each variable pair), and \code{"sumscore"} (the
distribution of the total score across variables). Defaults to all three.}

\item{ndraws}{Number of posterior draws, and therefore of replicated
datasets. Default \code{500}.}

\item{probs}{Numeric of length two; the predictive-interval quantiles.
Default \code{c(0.025, 0.975)}.}

\item{seed}{Optional integer seed for the replicate simulation.}

\item{cores}{Number of cores for the simulation. Defaults to
\code{parallel::detectCores()}.}

\item{display_progress}{Progress display passed to \code{\link[=simulate]{simulate()}}; one of
\code{"per-chain"}, \code{"total"}, or \code{"none"}.}

\item{...}{Passed to methods.}
}
\value{
An object of class \code{bgms_ppc}, a list with:
\describe{
\item{statistics}{A named list, one data frame per requested statistic,
with the element identifiers, the \code{observed} value, the predictive
mean \code{predicted}, the interval bounds \code{lower} and \code{upper}, and
\code{covered}.}
\item{coverage}{One row per statistic: the number of elements, how many
the intervals covered, and how many a calibrated model is expected to
cover.}
\item{ndraws, nsim, probs}{The settings the check ran under.}
}
}
\description{
Compares observed data summaries with their posterior predictive
distributions, drawing one replicated dataset per posterior draw so that
parameter and structure uncertainty propagate into the replicates.
}
\details{
The check reads on two layers. Coverage is the count of elements whose
observed value falls outside its predictive interval; with a 95\% interval,
about 5\% of elements land outside one under a model that fits, so a handful
of misses is the expected picture rather than a failure. The second layer is
the pattern: a systematic tilt or offset of the observed values against
their predictive means is misfit even when coverage looks unremarkable, and
it is what the plot shows and a coverage count cannot.

The statistics run from local to global. The margins involve one variable at
a time and are usually reproduced by any model that fits the data's
univariate shape. The pairwise correlations involve two. The sum-score
distribution depends on the joint dependence among all variables, so it is
the statistic that can fail while the other two pass.

Discrete variables only in this release; a fit containing continuous
variables errors, because these three statistics have no direct analogue
there.
}
\examples{
\donttest{
fit = bgm(Wenchuan[, 1:5], display_progress = "none")
check = posterior_predictive_check(fit, ndraws = 100, display_progress = "none")
check
plot(check)
}

}
\seealso{
\code{\link[=simulate.bgms]{simulate.bgms()}} for the replicate draws themselves,
\code{\link[=calibration_check]{calibration_check()}} for the conditional predictions

Other diagnostics:
\code{\link[=calibration_check]{calibration_check()}},
\code{\link[=plot.bgms_calibration]{plot.bgms_calibration()}},
\code{\link[=plot.bgms_ppc]{plot.bgms_ppc()}},
\code{\link[=plot.bgms_prior_sensitivity]{plot.bgms_prior_sensitivity()}},
\code{\link[=print.bgms_calibration]{print.bgms_calibration()}},
\code{\link[=print.bgms_ppc]{print.bgms_ppc()}},
\code{\link[=print.bgms_prior_sensitivity]{print.bgms_prior_sensitivity()}},
\code{\link[=prior_sensitivity_check]{prior_sensitivity_check()}},
\code{\link[=summarize_zratio_gauge]{summarize_zratio_gauge()}}
}
\concept{diagnostics}
