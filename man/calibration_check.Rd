% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration_check.R
\name{calibration_check}
\alias{calibration_check}
\title{Calibration Check}
\usage{
calibration_check(
  bgms_object,
  newdata = NULL,
  nrep = 200,
  probs = c(0.025, 0.975),
  grid_size = 101,
  seed = NULL,
  ...
)
}
\arguments{
\item{bgms_object}{A fitted model object of class \code{bgms} (from \code{\link[=bgm]{bgm()}}) with
discrete variables.}

\item{newdata}{Optional data to evaluate the predictions on, in the layout
\code{\link[=bgm]{bgm()}} was given. Defaults to the data the model was fitted to.}

\item{nrep}{Number of resampled datasets behind the consistency band.
Default \code{200}.}

\item{probs}{Numeric of length two; the band's quantiles. Default
\code{c(0.025, 0.975)}.}

\item{grid_size}{Number of points the curves are read off on. Default \code{101}.}

\item{seed}{Optional integer seed for the band resampling.}

\item{...}{Passed to methods.}
}
\value{
An object of class \code{bgms_calibration}, a list with:
\describe{
\item{curves}{A data frame with one row per variable and grid point:
\code{variable}, \code{grid}, the isotonic fit \code{pav}, and the band bounds
\code{lower} and \code{upper}.}
\item{summary}{One row per variable, worst first: \code{mean_dev} and
\code{max_dev}, the mean and maximum absolute distance of the fit from the
diagonal, and \code{share_outside_band}.}
\item{nrep, probs, grid}{The settings the check ran under.}
}
}
\description{
Reliability diagrams of the model's conditional predictions, one per
variable, with the consistency band a calibrated model would wander inside.
}
\details{
The band resamples each case's \emph{category} from its own predicted
distribution and refits the curve. It has to be built that way: the
cumulative threshold events of one case are nested, so resampling the events
independently would understate the band and make an ordinary wander look
like miscalibration.

Evaluated on the fitted data the check is in-sample, and the band is the
reference a model that is calibrated by construction produces on the same
data. Supplying held-out \code{newdata} makes it out-of-sample and strictly
harder to pass.

Calibrated conditional predictions do not imply that the model reproduces
the joint distribution: a model can predict each variable well from the
others and still understate how strongly they depend on one another. See
\code{\link[=posterior_predictive_check]{posterior_predictive_check()}} for the joint layer.

Discrete variables only in this release.
}
\examples{
\donttest{
fit = bgm(Wenchuan[, 1:5], display_progress = "none")
check = calibration_check(fit, nrep = 50)
check
plot(check)
}

}
\seealso{
\code{\link[=predict.bgms]{predict.bgms()}} for the conditional predictions themselves,
\code{\link[=posterior_predictive_check]{posterior_predictive_check()}} for the joint layer

Other diagnostics:
\code{\link[=plot.bgms_calibration]{plot.bgms_calibration()}},
\code{\link[=plot.bgms_ppc]{plot.bgms_ppc()}},
\code{\link[=plot.bgms_prior_sensitivity]{plot.bgms_prior_sensitivity()}},
\code{\link[=posterior_predictive_check]{posterior_predictive_check()}},
\code{\link[=print.bgms_calibration]{print.bgms_calibration()}},
\code{\link[=print.bgms_ppc]{print.bgms_ppc()}},
\code{\link[=print.bgms_prior_sensitivity]{print.bgms_prior_sensitivity()}},
\code{\link[=prior_sensitivity_check]{prior_sensitivity_check()}},
\code{\link[=summarize_zratio_gauge]{summarize_zratio_gauge()}}
}
\concept{diagnostics}
