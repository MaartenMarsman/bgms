% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_prior_inclusion_probabilities.R
\name{extract_prior_inclusion_probabilities}
\alias{extract_prior_inclusion_probabilities}
\title{Extract Prior Inclusion Probabilities}
\usage{
extract_prior_inclusion_probabilities(
  bgms_object,
  iter = 4000L,
  warmup = 1000L,
  recompute = FALSE
)
}
\arguments{
\item{bgms_object}{A fitted model object of class \code{bgms} from \code{\link[=bgm]{bgm()}}
run with \code{edge_selection = TRUE}.}

\item{iter}{Integer. Post-warmup iterations for the prior-only chain,
when one is needed. Default \code{4000}.}

\item{warmup}{Integer. Warmup iterations for the prior-only chain.
Default \code{1000}.}

\item{recompute}{Logical. Re-run the prior-only chain even when a
cached estimate is present. Default \code{FALSE}.}
}
\value{
A symmetric matrix of prior inclusion probabilities with the
variable names as row and column names and a zero diagonal, matching
\code{\link[=extract_posterior_inclusion_probabilities]{extract_posterior_inclusion_probabilities()}}.
}
\description{
Returns the prior edge-inclusion probabilities of a model fitted with
\code{\link[=bgm]{bgm()}} with \code{edge_selection = TRUE}, as a symmetric matrix in the same
shape and orientation as \code{\link[=extract_posterior_inclusion_probabilities]{extract_posterior_inclusion_probabilities()}},
so the two can be combined element-wise into prior and posterior
inclusion odds.
}
\details{
For models without a continuous block of at least two variables
(ordinal MRFs, mixed models with a single continuous variable) the
prior inclusion probability is the edge prior's marginal: the fixed
probability for \code{bernoulli_prior()}, \code{alpha / (alpha + beta)} for
\code{beta_bernoulli_prior()}, and the exchangeable partition mixture of
the within- and between-block means for \code{sbm_prior()}.

Under the joint spike-and-slab prior on a continuous block, the graph
marginal is reweighted by the per-graph normalizer — the
positive-definite-cone mass of the slab under the edge pattern,
further shaped by the determinant tilt — so the prior inclusion
probability of a continuous-continuous edge differs from the
edge-prior marginal at any \code{delta}, including \code{delta = 0}. For
\code{bernoulli_prior()} and \code{beta_bernoulli_prior()} it is read from the
same cached normalizing-constant table that corrects the fit's
hyperparameter updates (see \code{\link[=bgm]{bgm()}}); a fit whose slab family has no
table (beta-prime) falls back to a prior-only chain. For
\code{sbm_prior()} the reweighting also shifts the partition, and the
probabilities are estimated by a prior-only chain run with the fit's
own prior and correction settings; the estimate is cached on the fit,
and \code{recompute = TRUE} re-runs it.

In mixed models only continuous-continuous edges live in the joint
block, so the matrix carries up to three distinct values:
discrete-discrete, continuous-continuous, and cross edges. Variables
are a priori exchangeable within type, which makes the probabilities
constant within these classes.
}
\seealso{
\code{\link[=extract_posterior_inclusion_probabilities]{extract_posterior_inclusion_probabilities()}}, \code{\link[=bgm]{bgm()}}

Other extractors:
\code{\link[=extract_arguments]{extract_arguments()}},
\code{\link[=extract_category_thresholds]{extract_category_thresholds()}},
\code{\link[=extract_ess]{extract_ess()}},
\code{\link[=extract_group_params]{extract_group_params()}},
\code{\link[=extract_inclusion_bf]{extract_inclusion_bf()}},
\code{\link[=extract_indicator_priors]{extract_indicator_priors()}},
\code{\link[=extract_indicators]{extract_indicators()}},
\code{\link[=extract_log_odds]{extract_log_odds()}},
\code{\link[=extract_main_effects]{extract_main_effects()}},
\code{\link[=extract_pairwise_interactions]{extract_pairwise_interactions()}},
\code{\link[=extract_partial_correlations]{extract_partial_correlations()}},
\code{\link[=extract_posterior_inclusion_probabilities]{extract_posterior_inclusion_probabilities()}},
\code{\link[=extract_precision]{extract_precision()}},
\code{\link[=extract_rhat]{extract_rhat()}},
\code{\link[=extract_sbm]{extract_sbm()}}
}
\concept{extractors}
