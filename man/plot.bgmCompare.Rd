% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_bgms.R
\name{plot.bgmCompare}
\alias{plot.bgmCompare}
\title{Plot a Fitted bgmCompare Model}
\usage{
\method{plot}{bgmCompare}(
  x,
  type = c("difference", "groups", "centrality"),
  evidence_threshold = 10,
  group = 1,
  layout = "spring",
  legend = TRUE,
  ...
)
}
\arguments{
\item{x}{A fitted model object of class \code{bgmCompare}, from \code{\link[=bgmCompare]{bgmCompare()}}.}

\item{type}{Character; which display to draw. \code{"difference"} (default) is
the verdict-encoded network of group differences; \code{"groups"} draws each
group's own network and the difference panel on one shared layout;
\code{"centrality"} is the posterior strength centrality of
\code{\link[=extract_centrality]{extract_centrality()}}.}

\item{evidence_threshold}{Numeric > 1; the inclusion Bayes factor separating
evidence of a difference from undecided, as in \code{\link[=verdicts]{verdicts()}}. Default \code{10}.}

\item{group}{For \code{type = "centrality"}: a single group index, passed to
\code{\link[=extract_centrality]{extract_centrality()}} for that group's centrality. A difference in
centrality (two indices) can be extracted and summarized but has no plot;
see \code{\link[=extract_centrality]{extract_centrality()}} for the interpretation caveat. Default \code{1}.}

\item{layout}{Layout passed to \code{\link[qgraph:qgraph]{qgraph::qgraph()}}. Default \code{"spring"}.}

\item{legend}{Logical; draw the legend. Default \code{TRUE}.}

\item{...}{Passed to \code{\link[qgraph:qgraph]{qgraph::qgraph()}}, or to \code{\link[=plot.bgms_centrality]{plot.bgms_centrality()}} for
\code{type = "centrality"}.}
}
\value{
\code{x}, invisibly. Called for the side effect of drawing.
}
\description{
Draws the group differences the data settle, as one network, or the groups'
own networks beside it on a shared layout.
}
\details{
The default picture is about differences, because differences are what
\code{\link[=bgmCompare]{bgmCompare()}} parameterizes. A pairwise difference with evidence of presence
is drawn solid, its width scaled by the size of the difference and its colour
carrying the sign; an undecided difference is a thin dotted grey line; a
difference the data rule out is not drawn. The picture therefore says the
same thing as \code{verdicts(fit)} at the same threshold.

A network with no edges left is a result, not an error: "the groups do not
differ anywhere" is a common and correct finding, so the nodes are drawn on
their own and the subtitle says so.

Main-effect differences are not edges. When \code{main_difference_selection = TRUE} gave them their own indicators, their evidence is carried beside the
nodes: each node wears a probability wheel filled to its difference
indicator's posterior inclusion probability -- a full wheel is probability
1, half a wheel 0.5 -- coloured by the verdict (accented for presence, grey
for undecided, faint for absence). It is the same wheel
\code{\link[=plot_edge_posterior]{plot_edge_posterior()}} draws, so a probability looks the same on every
figure the package produces, and the filled fraction carries the number, so
the encoding does not rest on colour alone. Under the default
\code{main_difference_selection = FALSE} those indicators do not exist, so no
wheel is drawn and a note names the setting. \code{verdicts()} remains the place
to read main-effect differences precisely; the wheels are a summary of it.

The evidence is displayed exactly as \code{\link[=plot.bgms]{plot.bgms()}} displays an edge's: the
band above the network tallies the differences by verdict, names the
threshold on the natural-log Bayes factor scale, and prints the strongest
Bayes factor for and against a difference that the fit contains. Both
methods compose that band with the same code, so the two pictures cannot
come to report a Bayes factor differently.

Drawing needs the suggested package qgraph.
}
\examples{
\donttest{
fit = bgmCompare(
  x = Wenchuan[, 1:5],
  group_indicator = rep(1:2, length.out = nrow(Wenchuan)),
  display_progress = "none"
)
plot(fit)
plot(fit, type = "groups")
}

}
\seealso{
\code{\link[=verdicts]{verdicts()}} for the table the picture encodes,
\code{\link[=extract_centrality]{extract_centrality()}}, \code{\link[=prior_sensitivity_check]{prior_sensitivity_check()}}

Other posterior-methods:
\code{\link[=coef.bgmCompare]{coef.bgmCompare()}},
\code{\link[=coef.bgms]{coef.bgms()}},
\code{\link[=plot.bgms]{plot.bgms()}},
\code{\link[=plot_edge_posterior]{plot_edge_posterior()}},
\code{\link[=print.bgmCompare]{print.bgmCompare()}},
\code{\link[=print.bgms]{print.bgms()}},
\code{\link[=print.bgms_verdicts]{print.bgms_verdicts()}},
\code{\link[=summary.bgmCompare]{summary.bgmCompare()}},
\code{\link[=summary.bgms]{summary.bgms()}},
\code{\link[=verdicts]{verdicts()}}
}
\concept{posterior-methods}
